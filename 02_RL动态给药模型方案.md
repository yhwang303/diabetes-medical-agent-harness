# RL 动态给药模型方案：预测增强的模型式策略学习

> 当前研究版本：P03 DSENet（global12/3、local2/1）+D05 world+D06有限候选RL，单seed260915；主表TIR97.08±4.50、TBR70 1.55±3.89、TBR54 .57±1.79。最新正文比较含8种外部算法及hold，见[正文报告](RL_DSENet_2026-09-17/revision/论文正文对比报告.html)与[交接](RL_DSENet_2026-09-17/revision/交接与证据.md)。保留单seed、额外仿真监督和个体低糖边界，不称全指标SOTA；Core/Harness产品准入不变。


> 2026-09-16 最新目标覆盖：以RL-DITR完整患者模型、策略/价值和beam规划为基底，先跑通后研究近期论文支持的创新；O07/ReBRAC不再作为我方主线。新训练仅seed260915、保留全量Loop及稀疏food/exercise；FQL/ReBRAC不再运行。多维评价与新场景先冻结，已有10个模拟成人均已曝光。任务未完成，见[本轮合同](RL_DITR创新_2026-09-16/任务与复现合同.md)。


> 2026-09-16 范围澄清：最终O07是固定参考有界残差ReBRAC，属于incremental任务适配；RL-DITR模型式路径的早期尝试未成为最终模型。仿真研究门槛通过不代表非incremental创新、SOTA或产品完成；M02d保留未完成。具体结构/损失/指标及ICML预测器接入边界见[模型说明](RL进阶对比_2026-09-15/模型结构与读表说明.html)。


> 2026-09-15 22:14 最新研究结论：O07固定历史参考±0.4 U/h残差ReBRAC（seed260915、50k、全部225训练患者）通过冻结后的6名独立模拟患者研究门槛，TIR94.97%、TBR70 2.05%、TBR54 0.52%、0/54终止；仅可作为Harness受控研究接入候选。adult009低血糖仍明显、只有单seed，未证明临床安全。独立JSON推理及Mac权重一致性已验；未注册Core，M02产品父项/M02a及双模型准入保持未完成。最终结果、失败尝试和论文表见[训练审核报告](RL进阶对比_2026-09-15/训练审核报告.html)。此条覆盖下方历史阶段的“尚未训练/策略未启动”状态，不删除其负结果。


> 2026-09-15 21:49 用户最新覆盖：所有模型均只训练一个seed，主比较使用260915；已停止O07额外seed进程，未启动的下一seed撤出。此前多seed记录仅作历史证据，不再列为当前必做要求。后续同一批独立评价情景用于所有方法，不涉及重新训练；单seed不证明随机初始化稳定性。


> 2026-09-15 21:41 用户最新研究范围：外部ReBRAC/FQL baseline各用一个训练seed的同数据可比结果即可，后续重点优化我方模型；未提交的额外baseline seeds已撤出。我方候选继续稳定性与独立评价。当前为个人历史参考+有界残差结构、行为/价值损失权重及去RL消融，全部225训练患者和1,653,421起点保留，性能阈值未改变。实际结果以[进阶训练审核报告](RL进阶对比_2026-09-15/训练审核报告.html)和研究协议最新条目为准；原方向比例不是验收门槛，真实双模型/Harness产品准入仍未完成。


> 2026-09-15 20:28 持续目标更新：用户已明确授权2–3个较新RL基线公平对比、有论文依据的损失/risk/模型结构实质改进、多seed训练和独立仿真，最终交付全部实验HTML与论文式对比表。当前ReBRAC/FQL真实离线策略及BC已实现并在AutoDL运行50k pilot；新实验入口为[研究协议](RL进阶对比_2026-09-15/研究协议.md)。旧80%动作方向门槛仅保留诊断，不作为新策略验收；独立闭环合同已在成绩产生前冻结。Loop全部225训练患者/1,653,421起点和food/exercise保持，旧轮次权重负结果保留；不能将新策略已启动写成旧轮次已有完整RL。当前总目标仍未完成，M02a/M02未勾选。此条覆盖下文旧仅复现不创新和停止策略训练的当前执行限制；产品可信规则与Harness闸门不修改。


> 2026-09-15 当前执行：用户已授权在AutoDL连续训练并按结果调整，使用[22维训练管线v2](Loop数据集/训练管线_v2/README.md)，food/exercise只加特征和mask，225名train/1,653,421起点不减少。实际训练及失败结果见[训练审核报告](RL训练_2026-09-15/训练审核报告.html)；本轮实验已结束：S00烟测、A01失败、A02/A03各完整1epoch及全量validation已验证。A03的30/60分钟RMSE为0.9973/1.6775，但+0.1U/h仅2.50%诊断样本预测下降；当前权重不能用于RL给药，策略阶段因患者模型动作响应未通过而未启动。M02a/M02仍未完成，Harness未接入真实模型。此条覆盖下文旧14维默认训练/未有SSH等状态。

> 2026-09-14用户最新确认：**先按RL-DITR原文与作者代码复现网络、训练目标及规划流程，跑通研究闭环，再根据结果讨论结构/损失创新。** 当前不加入DPO、门控或其他自创目标，也不以自选Actor–Critic简化损失冒充原文实现。Loop成人T1D泵与原文住院T2D注射的输入、动作、时间尺度差异属于必要任务适配，逐项记录；不宣称复现原临床实验。已有独立预测器F继续保留在产品双模型设计中，不默认能直接替换患者模型内部fP。

> 2026-09-14当前方法与数据入口：[训练管线v1](Loop数据集/训练管线_v1/README.md)。主线按RL-DITR的历史表征、动作条件患者模型、血糖/WTR预测、历史监督与模型式策略学习推进。5分钟是观测网格，不是论文规定的给药周期；已实现5/15/30/60分钟恒定基础率样本视图，最终产品动作周期未冻结。默认核心输入为72×14（3个数值通道及掩码/时间），餐食/运动因录入时效未知仅提供显式回顾性可选模式。奖励按作者固定commit代码解决原文括号差异；真实网络、闭环效果和CUDA训练均未验收。

> 2026-09-14批次44最新：已完成 [Loop无插值预处理与最终审计](Loop数据集/预处理_v1/00_从这里开始.md)。实际train为225人/1,653,421条transition；validation55人、sealed_test32人、demo17人。329是四组合计。原始文件未改；真实模型与反事实/临床闭环未验收，D01父项仍未完成。下文此前“未处理/369候选”属于历史状态。

2026-09-12 批次38最新结论：[真实数据集RL训练资格审计报告](07_真实数据集RL训练资格审计报告.md)。五来源已解压并完成关键表全量审计；Loop为唯一首版RL/生理主池候选（369为提名上限，非合格人数），DCLP3/Ohio/UOM用途分层，IOBP2整源外部部分评价。先资格冻结，再BC/TD3+BC和ReplayBG生理结构辨识，通过独立验证后开展模型式RL；当前Harness须新增真实来源/basal+bolus合同。未正式处理、未训练或改产品实现，M01/M02未完成。以下旧来源分工与下一步安排由本条及07覆盖；历史要求保留。

更新：2026-09-11｜v6.1：真实联合轨迹训练患者模型与RL，公开仿真作为辅助评价｜状态：模型方案，尚未训练或完成运行验证。

配套：[项目总体方案](01_项目总体方案.md)｜[原长版方案（保留）](03_原长版方案_v3.5_保留.md)

2026-09-11 批次28最终调整：数据构建与近期顺序以 [RL真实联合数据集下载与构建方案](06_RL真实联合数据集下载与构建方案.md) 为准。用户先下载 Loop、DCLP3、UOM，并申请 Ohio/DiaTrend；取得原包后先审资格。主训练来自真实成人T1D泵轨迹，simglucose只作辅助基线；现有合成/仿真执行合同不因此开放真实历史或真人用途。原训练/评价要求保留，M01/M02及全量数据质量均未完成。

## 1. 已经确定的方向

**当前先复现RL-DITR的方法基线：按原文与作者代码实现历史表征、动作条件患者模型、策略/价值、联合训练目标和多步规划，完成研究闭环后再研究改进。** 原文/代码差异需核对记录，不默认为可自行替换的创新空间。保留CGM与Loop成人T1D泵任务，不复现住院T2D临床队列或原临床结果，不将私有队列/作者权重作为开工前提。

研究任务为成人T1D基础输注，先对真实联合轨迹开展回顾性患者模型和策略研究，再做独立评价及非临床仿真闭环。预测器的人群、采样、历史/预测窗口与训练患者重叠必须核对，不能默认为通用。新策略不作用于真实泵、不向真人发布剂量；真实患者用途仍须满足总体方案的产品与医学准入。

```text
Loop合格轨迹 → RL-DITR方法基线：历史表征/动作条件患者模型/策略与规划
                         ↓
                 独立研究闭环验证 → RL服务
                                      ↓
独立预测器F → 预测证据 ───────────── Harness → App

基线闭环完成且结果明确之后 → 再立项讨论结构、损失或DPO等改进
```

此前 **5–7 周**为仿真主线估算；真实联合数据路线须等访问、有效轨迹和训练吞吐确认后重估。先完成原包资格/合同和基线，再研究算法改动。算法未带来收益时保留可靠基线、如实呈现结果；不能预先保证创新方法胜出。

## 1.1 首版胰岛素任务选择与产品衔接

**用户已明确：RL-DITR 仅供学习方法，不复刻其临床任务、实验设计或私有医院队列；本项目只使用公开数据集和公开仿真资源。** 不将取得原作者数据/权重、医院合作或复现论文数值作为开工和研究交付前提。公开资源若有 DUA、申请、用途或再分发限制，按条款取得和使用，不等于无条件开放。本节是本项目自己的任务选择，不从论文的任务或结论推导产品必然有效。

2026-09-10 用户将“胰岛素剂量建议”的首版范围交由我们结合公开资源与模型决定。本次选择 **成人 T1D 的基础输注建议，先完成同一虚拟患者上的真实模型研究闭环**。理由是现有 CGM 方向可衔接，公开生理仿真器能让不同胰岛素动作产生不同后果，因而能训练并评价策略；不是因为基础输注被证明是最佳临床选择。预测器是否适配该域仍是必须完成的验收，当前不能默认通过。

| 方案 | 公开证据和开发条件 | 本次决定 |
| --- | --- | --- |
| 成人 T1D 泵基础输注 | simglucose 提供成人虚拟患者、基础输注动作和反馈；可生成有行为与后果的公开可复现实验轨迹 | 首个研究任务；真实模型通过后提供明确标记的基础率、持续时间和区间总量建议 |
| 成人 T1D 餐时/纠正 bolus 或日常多次注射 | 公开历史记录可支持字段审计，但新动作后果、餐食、既有胰岛素作用及不同制剂需另设模型与评价 | 不纳入第一项策略；不能用基础率积分冒充一次注射剂量；扩展需独立立项 |
| T2D 胰岛素方案调量 | 可探索其他公开 T2D 资源，但本次未证明存在适合目标动作且可用的训练/独立评价链 | 暂不转向；不等待或申请 RL-DITR 私有医院队列，也不把公开样例当充分训练数据 |

批次28更新资源分工：Loop+DCLP3合格成人轨迹作为开发主体，Ohio+UOM提供合格多模态补充，DiaTrend优先整体作为外部来源留出；simglucose保留辅助仿真。上表是既有动作范围的确定历史，不再表示必须以仿真生成数据为主训练。CGMacros仅用于其自身人群辅助任务。来源可合并患者轨迹，不能跨患者补齐事实。历史相关性不自动提供可信反事实，公开仿真也不等于临床证明。

依据：[simglucose 项目](https://github.com/jxx123/simglucose)、[已核对的基础输注 wrapper](https://github.com/jxx123/simglucose/blob/a6f6777586c860457a2a1e6cce6d0bd3fd33d1cc/simglucose/envs/simglucose_gym_env.py)、[泵速率实现](https://github.com/jxx123/simglucose/blob/a6f6777586c860457a2a1e6cce6d0bd3fd33d1cc/simglucose/actuator/pump.py)、[DCLP3 公共申请入口](https://public.jaeb.org/dataset/573)、[OhioT1DM 作者入口](https://webpages.charlotte.edu/rbunescu/ohiot1dm.html)、[CGMacros 数据说明](https://physionet.org/content/cgmacros/1.0.0/)。此次仅核对公开说明和相关源码，没有下载队列、取得许可或验证真实预测器，不把这些状态写成完成。

产品动作合同必须先于工具接口冻结：保留模型原生基础率数值及单位，U/min 与 U/h 可由受控代码等价转换，持续时间和该时间段总量 U 全部绑定同一动作及模型证据；单位表示不同不直接拒收，未知单位或缺少动作语义不能猜测转换；短周期候选不能外推为全天或转换成注射处方。真实制剂、浓度、途径、装置和既有治疗方案必须在相应验证配置中明确；通用仿真胰岛素不能冒充某一真实药品。药物选择本身不由 LLM 从检索资料临时决定。

数值模型每个仿真步运行不意味着每 5 分钟调用一次 LLM 或要求用户操作；训练/仿真评价和交互 Agent 分开。用户建议的有效期必须与模型窗口及实际处理延迟一致，超时不能发布。用户自报“已执行”与测得的实际递送保持不同来源；建议值不自动回填为实际用药，后续观测与实际行为生成新快照。

## 2. 借鉴论文的哪些部分，哪些重新设计

依据：Wang 等，*Optimized glycemic control of type 2 diabetes with reinforcement learning: a proof-of-concept trial*，Nature Medicine，2023，DOI 10.1038/s41591-023-02552-9。[用户提供的完整论文](参考论文/RL-DITR_NatureMedicine_2023.pdf)｜[正式论文](https://www.nature.com/articles/s41591-023-02552-9)｜[作者仓库](https://github.com/rlditr23/RL-DITR)

论文采用历史编码、动作条件状态转移、血糖/WTR 预测、策略/价值学习，以及监督与模型生成经验等训练信号。原任务是住院 T2D、每日七个不等距时段的注射调量；开发队列 12,981 人，临床可行性试验 16 人、单臂设计。论文不是分钟级泵控制效果证明，其数值结论不能直接转移到本项目。

| 论文部分 | 本项目处理 |
| --- | --- |
| 历史编码与状态表征 | 保留职责；按用户最新要求以原文3层Transformer、宽256、8头作为待训练的结构起点，GRU留作可选小模型对照 |
| 动作条件转移模型 | 保留职责；用合格真实联合轨迹学习患者模型，检验多步误差；仿真轨迹仅用于独立标记的辅助实验 |
| 预测模块 | 你的预测器提供历史条件趋势；患者模型自行产生受动作影响的下一观测 |
| 策略与价值网络 | 首先使用小 MLP；不要求 Transformer |
| 历史监督、模拟学习 | 先逐项对照原文和作者代码的历史/模拟RL项及监督项；歧义写明来源和解决方式，不静默改成其他目标 |
| ClinicalBERT 病历特征抽取 | 移出数值 RL 主链。App 需要时用 DeepSeek/Qwen 抽取，经来源、时间、单位、否定语义校验后入库 |
| Beam search、多步推理规划 | 纳入论文方法基线的复现范围；实现/时域适配及计算预算须核对，未实现不称已完成原文规划链 |
| 作者权重、代码、原始临床数据 | 可供理解和对照，不是实现、训练或交付依赖 |

ClinicalBERT 在原文中负责特征抽取，不是给药策略的核心。外接 LLM 后可以省去它，但不能认为 LLM 已自动完成可靠抽取，更不能让 LLM 替代数值动力学模型。数值 RL 实验可以完全不调用 LLM。

我们按公开公式与结构独立实现，写清论文与本实现的映射、差异及假设，引用思想来源；只有确实复现的设置才使用“复现”一词。自行编写代码本身不构成算法创新，创新必须落在明确机制及其对照证据上。复用第三方代码/数据时仍分别遵循许可。

## 3. 预测器、患者模型和策略各自负责什么

### 3.1 独立预测器F与患者模型内部fP不能默认直接替换

用户已确认：现有预测器只根据历史预测未来血糖，不接收未来候选给药动作。

记可用历史为h_t，独立预测器为F，输出F(h_t)。论文fP接收经fR编码、fT按候选动作推进后的潜在状态；不同候选动作的后果由M(h_t,a_t)生成，不能拿同一条F(h_t)曲线给不同剂量评分。当前14维输入没有F预测特征；此前“直接融合趋势特征”属于待做集成，未实现。

首轮论文方法基线保留内部fR/fT/fP结构。产品层保留F预测服务与RL服务的双证据合同；RL服务内部含患者模型，不等于增加第三个对用户独立给结论的服务。若后续将F的编码器/权重复用到内部患者模型，须核验真实输入、潜在空间、输出窗口、动作条件与训练来源，并重新训练/验证连接部分；不能把更高的历史预测精度直接当作反事实预测能力。F加入actor趋势特征同样作为明确的后续集成变更。

### 3.2 最小网络配置

| 模块 | 起点 | 验证重点 |
| --- | --- | --- |
| 冻结预测器 F | 你的现有模型，固定版本与预处理 | 适用人群、窗口、仿真域误差、策略改变后的分布偏移 |
| 历史编码 | 按原文起点：3层Transformer、宽256、8头；输入mask和时间间隔 | 对近期胰岛素和延迟作用是否有足够历史支持；尚未训练 |
| 患者模型 M | 历史编码后以动作/持续时间推进潜在状态，使用血糖/WTR预测头；参考原文动态Transformer与MLP职责 | 一步、多步误差与动作优劣排序，不读取未来真实状态作为模拟输入 |
| 策略与价值 | 按原文起点：各3层MLP、宽256；连续基础率分布需独立实现，不能照搬注射剂量类别 | 有界动作分布、log probability、值函数及训练稳定性 |
| 集成 | 三个独立初始化、按患者/完整轨迹重采样的患者模型 | 分歧是否反映决策错误；模型共同错误的失败例 |
| GRU小模型 | 可选同输入的容量/速度对照 | 按真实验证结果比较，不预先保证Transformer更优 |

先确定一个小配置跑通，尺寸属于开发集调参项，不是已证明最优。策略历史应包含已执行胰岛素及足够长的作用历史；IOB 如使用，由有来源的固定模型计算，不能由 LLM 猜测。

### 3.3 模型展开中的历史怎么更新

真实观测根状态使用截至 t 的可用 CGM、动作和 F(h_t)。在模拟分支内，M 生成下一 CGM，动作及确定性时钟更新历史，再对这段**混合实测与模拟的历史**调用冻结 F，得到下一模拟状态的特征。全程标记为模型生成数据，绝不使用未知的未来真实 CGM，也不注册成 App 的真实预测产物。

训练和 rollout 均使用同一历史构造合同。固定模型后可批量计算 F；测量吞吐与复合误差，不能假设冻结 F 在模拟历史上仍校准。初期比较 2/4/8 个决策步的短展开，配合统一末端价值。短展开用于限制误差积累，**不代表已经覆盖胰岛素的全部延迟作用**；最终必须用多日闭环检验。

患者模型只学习策略实际可见的动态量。未来餐食是否告知策略、已申报餐食怎样输入须固定；未申报的未来餐食只能由环境生成，不能作为 actor 的特征。隐藏生理参数供仿真器推进和评估使用。

## 4. 数据集与仿真资源：选哪些，怎么拿

### 4.1 推荐优先级

**当前已取得Loop、DCLP3、UOM、Ohio2018+2020及IOBP2，DiaTrend暂停；审计后只有Loop为首版RL/生理主池候选，其余按07用途分层。** 版本/入口/字段/外部留出及备用规则见 [06方案](06_RL真实联合数据集下载与构建方案.md)。下表保留既有候选资料；未列入首轮的来源不作为下载或训练依赖。

| 资源 | 内容及取得方式 | 本章用途与限制 |
| --- | --- | --- |
| simglucose | 作者公开 Python 仿真器，含成人/青少年/儿童虚拟人群 | 辅助仿真基线，非真实主训练集或唯一效果评价；仿真研究只选成人。[仓库](https://github.com/jxx123/simglucose) |
| Loop Observational Study | Jaeb公开表单；2023-01-31原包，含CGM/基础/bolus/食物及受试者资料 | 与DCLP3作为主体资格核验；必须筛成人和连续有效事件，不把845个整理ID当可用成人数。[入口](https://public.jaeb.org/dataset/560) |
| DCLP3 | Jaeb 公开申请/填表与条款后取得；原研究 168 人、14–71 岁、26 周 | 有 CGM 与泵记录，优先真实数据适配。原研究总人数不等于实际可用泵数据人数；成人要单独筛选。[Release 3](https://public.jaeb.org/dataset/573) / [原研究](https://pmc.ncbi.nlm.nih.gov/articles/PMC7076915/) |
| DCLP5 | Jaeb 公开申请；原研究 101 名 6–13 岁儿童、16 周 | 有儿童闭环数据，但首版成人策略不直接使用；后续域外分析需独立设计。[入口](https://public.jaeb.org/dataset/535) / [原研究](https://pubmed.ncbi.nlm.nih.gov/32846062/) |
| OhioT1DM | 12名T1D，约8周/人；CGM、基础/临时基础/bolus、餐食及部分生理信号 | 详细作者页要求机构邮箱向作者申请，旧页面仍列DUA流程；按详细页请求并取得当前适用许可。合格轨迹作多模态补充。[详细入口](https://webpages.charlotte.edu/rbunescu/data/ohiot1dm/OhioT1DM-dataset.html) |
| CGMacros 1.0.0 | 45 人，健康/糖尿病前期/T2D，双 CGM、营养/照片/活动/实验室信息；PhysioNet 开放，CC BY-NC-SA 4.0 | 最适合真实配对多模态演示；**不是 T1D 泵给药训练集**。其中 Insulin 为实验室空腹胰岛素，不是注射剂量。[主页](https://physionet.org/content/cgmacros/1.0.0/) / [Bio 字典](https://physionet.org/content/cgmacros/1.0.0/DataDictionary_Bio.csv) |
| MetaboNet | 新整合资源，作者报告 3,135 人和约 1,228 患者年，含 CGM/胰岛素，多源访问条件 | 候选扩展，不作为首月依赖；整合统计不表示全部组件能无条件下载，先核对版本/来源重叠。[预印本 v2](https://arxiv.org/abs/2601.11505v2) / [项目](https://metabo-net.org/) |
| BIG IDEAs 1.1.3 | 16 人、CGM/腕带/食物记录；ODC Attribution 1.0，1.1.3 修正食物日期错位 | 多模态备选，非胰岛素 RL 主数据；文件较大无需先下载。[主页](https://physionet.org/content/big-ideas-glycemic-wearable/1.1.3/) |

### 4.2 DCLP 的关键发现：有泵数据，仍不能偷换字段含义

UVA 糖尿病技术研究机构公开的 [DIAX 转换项目](https://github.com/Center-for-Diabetes-Technology/DIAX) 为字段提供了比“论文说有胰岛素”更直接的证据：DCLP 转换器读取基础率变化记录的 `CommandedBasalRate`（U/hr），以及 bolus 完成/递送记录的 `BolusAmount`（U）。

- [固定提交的 DCLP3 转换器](https://github.com/Center-for-Diabetes-Technology/DIAX/blob/b807a99b33a0cbb4c2e1f8f3d75a6b74d6d8e3ed/scripts/dclp3.py)：`Pump_BasalRateChange.txt` 与 `Pump_BolusDelivered.txt`。
- [固定提交的 DCLP5 转换器](https://github.com/Center-for-Diabetes-Technology/DIAX/blob/b807a99b33a0cbb4c2e1f8f3d75a6b74d6d8e3ed/scripts/dclp5.py)：`DCLP5TandemBASALRATECHG_b.txt` 与 `DCLP5TandemBolus_Completed_Combined_b.txt`。

DIAX 是机构整理实现，非 Jaeb 原始字典，其说明不等于研究 sponsor 背书。本轮未找到足以确认可直接复制该代码的明确 LICENSE；读取语义不等于获得复制许可。

因此需要区分：**命令基础率、由持续时段重建的输注暴露、设备确认的 bolus**。不能把编程基础率当独立核验的实际递送量。暂停、泵故障、跨天持续、自动模式和延长 bolus 的处理，要结合原数据字典与样例核对。转换器没有输出 carbs，不得默认碳水字段已齐备。

Jaeb 原包本轮没有提交申请或下载；字段证据来自公开机构代码，不是已审完全部原包。先取得 1–3 名允许使用的样本及字典做数据审计，再决定是否值得增加离线 RL。各整理项目过滤人群不同，不能把原 RCT 168、其他整理版人数直接当同一口径。

### 4.3 新补充的开放数据

| 资源 | 获取与内容 | 应怎样使用 |
| --- | --- | --- |
| T1D-UOM | 作者公开仓库、JSON schema、CC BY 4.0；CGM、基础/bolus、营养记录 | 适合小型真实输入示例；混合泵和长效注射，不能统一视为基础输注。[仓库](https://github.com/sharpic/ManchesterCSCoordinatedDiabetesStudy) / [Zenodo](https://zenodo.org/records/15806142) |
| AZT1D v1 | 25 名 T1D，Mendeley，CC BY 4.0，含 CGM/bolus/碳水 | 基础率由小时 PDF 图 OCR 后重复填入 5 分钟行，不能当精确 5 分钟实测递送。[数据](https://data.mendeley.com/datasets/gk9m674wcx/1) / [原文](https://arxiv.org/html/2506.14789v1) |
| HUPA-UCM v1 | 25 名 T1D，Mendeley，CC BY 4.0，CGM/胰岛素/碳水/活动 | 尚未核实逐列字典，暂不用于精确基础输注主实验。[入口](https://data.mendeley.com/datasets/3hbcscwz44/1) |

T1D-UOM 的 [schema](https://github.com/sharpic/ManchesterCSCoordinatedDiabetesStudy/blob/main/JsonSchema.json) 与 [许可证](https://github.com/sharpic/ManchesterCSCoordinatedDiabetesStudy/blob/main/LICENSE.md) 已核验。CGM 为 mmol/L；basal 可能是注射 U，也可能是速率 U/h，必须同时读 `insulin_kind`。2026-09-11 补查 V1.0.4：论文 17 成人中 7 泵、9 MDI、1 未知；仅 6 名泵用户有 basal 文件。已取两份公开 basal 样例，发现同刻多值及 DD/MM/YYYY 与说明冲突；未确认完整实际递送轨迹，不能把 17×90 天当有效训练规模。根目录 CC BY 4.0 完整，1 字节许可文件属于 OAuth 子目录。证据见 [审核报告](05_公开数据集与RL闭环可行性审核.md)。

AZT1D 的 `TotalBolusInsulinDelivered` 是 U，`CarbSize` 是 g；`FoodDelivered` 是覆盖食物的胰岛素，不是碳水。其缺事件补零、基础率 OCR 和时间网格处理会影响离线学习，必须保留这些标记，不宣称提供无噪声动作真值。

MetaboNet 的 [公开字典](https://metabo-net.org/data-dictionary) 将 basal/bolus 定义为前 5 分钟量 IU，数据入口要求登录。[作者处理仓库](https://github.com/replicahealth/metabonet_processor) 说明若干受控源仍需独立许可。统一字段不证明 DCLP 命令率已变成实际递送，必须追溯 `source_file`。恒定 U/h 转 5 分钟 U 可除以 12，但只在时间与输注假设成立时使用；整理版与原始来源不算独立外部验证。

取数顺序：先读开放 schema 和少量允许使用的样例，核实必需字段交集，再申请 DCLP/Ohio，只下载所需版本。软件公开许可证不能自动覆盖其中所有来源数据。

### 4.4 最小数据审计清单

- ☐ 官方版本、许可/DUA、再分发限制、原始文件 hash；不自动代签条款。
- ☐ 患者类型、年龄、研究臂、访视/随访关系；聚合源和原源按 subject/study 去重。
- ☐ CGM 单位、原始采样频率、时间戳与日期平移、时区、重复/缺失。
- ☐ 剂量是 U 还是 U/hr，命令还是递送，是否有暂停、延长 bolus、泵模式。
- ☐ 餐食事件发生时间与录入时间分开；不能把事后补录信息当决策时已知。
- ☐ IOB/COB 是模型估计，不由 LLM 补写；药效参数来源及缺失处理可追溯。
- ☐ 不跨长缺口插值构造 RL transition；状态、动作、下一状态有明确对齐。
- ☐ 标准化只拟合训练集；患者划分先于切窗，隔离重叠窗口。

Ohio 的体重 `99` 是占位，不可当真实体重；不同年份可穿戴信号不同。CGMacros 的对齐表格是 1 分钟网格，非两种 CGM 都原生 1 分钟；HbA1c 字典单位与数值范围有疑点需确认，部分血脂异常标记不能当真实测量值。真实照片必须按原 subject/time 配对，不能混拼成“完整患者”。

## 5. 环境、数据与评价协议

**批次28范围说明：真实主训练集与划分按06方案执行。以下5.1–5.3的虚拟成人划分、bolus关闭、规则控制器生成轨迹和分支恢复要求只用于保留的辅助仿真实验，不用于删去真实轨迹中的bolus。** 真实患者模型读取独立的历史基础/bolus暴露；actor仍只输出基础率。辅助仿真任务与真实混合治疗的分布差异须报告。真实外部预测/OPE与仿真闭环分别评价，不以一项替代另一项。

### 5.1 固定一个控制任务

首版：成人 T1D 虚拟患者、固定 5 分钟研究决策间隔、单个基础输注速率动作；餐时 bolus 关闭，餐食是扰动。这是有限的控制研究任务，不能据此声称完整临床治疗能力。若以后使用固定餐时控制器，所有比较组同步采用同一版本，并分清 RL 与规则动作来源。

simglucose 含 30 名虚拟患者，其中成人 10 名；先用训练池内 3 名做工程检查，再固定例如 6/2/2 的训练/验证/封存测试成人划分。样本很小，需报告个体表现与不确定性，不能把同一患者多个窗口算独立患者。测试病例、扰动和随机种子不参与调参。第一周确认环境配置和吞吐后锁定多日评价时长。

来源：[simglucose 仓库](https://github.com/jxx123/simglucose)。固定核验源码：[泵](https://github.com/jxx123/simglucose/blob/a6f6777586c860457a2a1e6cce6d0bd3fd33d1cc/simglucose/actuator/pump.py)、[Gym wrapper](https://github.com/jxx123/simglucose/blob/a6f6777586c860457a2a1e6cce6d0bd3fd33d1cc/simglucose/envs/simglucose_gym_env.py)、[环境](https://github.com/jxx123/simglucose/blob/a6f6777586c860457a2a1e6cce6d0bd3fd33d1cc/simglucose/simulation/env.py)。公开 Python 实现不能称为“获 FDA 批准的控制系统”。

### 5.2 动作、奖励和数据必须有确定含义

- 仿真泵基础率接口为 U/min；数据可能是 U/hr 或区间总量 U。实现显式转换、持续时间与量化检查，绝不混用。
- 策略使用有界连续分布；记录原动作、变换后速率、泵执行速率和区间剂量。log probability 对应采样变量/变换分布，不能拿泵裁剪后的值冒充原采样动作。
- 动作上下限、变化范围、拒绝条件属于固定实验配置和规则版本，不能让 LLM 修改。拒绝返回 null 与原因；0 只表示有效的零输注动作。
- 起始奖励采用固定版本的仿真血糖风险函数，其量纲、极值处理、失败惩罚和聚合方式在第一周落实。所有组一致，不边看测试表现边换奖励。不能只优化平均血糖而忽略低血糖风险。
- 对失控/数值故障记录失败和截断原因；不能删掉失控轨迹后报告更好的 TIR。终止与时间截断区别处理，必要时报告剩余时长的失败占比。

### 5.3 训练轨迹与独立评测

训练数据来自固定规则控制器及受边界限制的扰动策略，在训练虚拟患者上产生；保留行为来源、种子、动作支持范围及完整轨迹。行为克隆参考策略 π_b 从这些数据学习，它只是行为参考，不称为“医生策略”或安全保证。

真实历史数据用于预测器适配、输入审计、行为对照和演示；满足动作语义、覆盖、混杂及许可条件的子集也可用于患者模型和离线 RL，具体须先通过数据资格审核。新策略动作必须改变环境后续血糖。直接 replay 真实 CGM、同时允许 RL 随意改动作，不构成有效闭环评价。没有额外因果假设与覆盖检验，不宣称真实离线数据证明给药疗效。

评价在**与学习到的患者模型分离的生理仿真器**执行，不让患者模型给自己验收。训练轨迹与评价仍来自同一仿真器家族，必须披露该限制；留出患者/餐食/噪声并不等于跨仿真器或临床验证。

配对实验预生成外生餐食与噪声序列，分离策略采样 RNG；比较分支需还原完整生理、历史、传感器、泵与随机状态。验证“相同动作恢复同一轨迹”，不能只调用 reset(seed) 就假定分支相同。

## 6. 算法主线：决策优势一致性门控

> **本节改为后续研究备选，当前不执行。** 用户要求先复现论文基线并跑通闭环；以下旧创新设计及“优先门控”的表述仅保留以供追溯，不作为当前训练要求。DPO也仅作概念讨论，未获当前实施指令。

### 6.1 要解决的问题和创新边界

模型式 RL 容易利用患者模型的误差，学到“模型中有效、真正执行较差”的策略。只看预测误差或方差未必能判断一个动作是否值得学习；我们重点检验：**多个患者模型是否一致认为候选动作比参考动作更好或更差？**

拟研究的改动是对这种“动作优劣一致性”进行门控，只让方向一致的模拟决策信号参与对应 actor 更新。这个训练门控与产品 Harness 是两层机制：前者是学习启发式，后者是来源、权限、合同及发布约束。不能把训练门控当作医学安全证明。

已有 [MOPO](https://proceedings.neurips.cc/paper/2020/file/a322852ce0df73e204b7e67cbbef0d0a-Paper.pdf)、[MOReL](https://papers.nips.cc/paper/2020/file/f7efa4f864ae9b88d43527f4b14f750f-Paper.pdf)、[COMBO](https://proceedings.neurips.cc/paper/2021/file/f29a179746902e331572c483c45e5086-Paper.pdf)、[CBOP](https://ssanner.github.io/papers/iclr23_cbop.pdf) 研究模型误差与保守学习；[MOWPO](https://ojs.aaai.org/index.php/AAAI/article/download/27056/26828) 是相关置信度加权的 student abstract。不能把“不确定性加权”或“加一个门”直接称首创。短模型分支亦有 [MBPO](https://papers.nips.cc/paper_files/paper/2019/file/5faf461eff3099671ad63c6f3f094f7f-Paper.pdf) 等先例。此处是可验证的毕设算法候选，尚未建立优先权或可专利性结论。

### 6.2 可实现的最小目标

先训练并冻结三个患者模型 M_m 和行为参考 π_b。每轮冻结采样策略 π_old、末端价值 V_target；从训练轨迹取一个观测根状态 x，候选 a 从 π_old(.|x) 抽取，参考 a_ref 由固定 π_b 确定。

每个 M_m 内分别展开两条分支：第一步执行 a 或 a_ref，之后均使用 π_old；用相同的展开长度和配对的外生随机条件。后续策略相同不代表两条分支动作相同，因为状态可能不同。

```text
Qhat_m(x, a) = sum(j=0..K-1) gamma^j * rhat_m,j
              + gamma^K * V_target(xhat_m,K)
Delta_m      = Qhat_m(x, a) - Qhat_m(x, a_ref)
w            = 1[min_m Delta_m > delta OR max_m Delta_m < -delta]
A_sim        = mean_m Delta_m
rho          = pi_theta(a|x) / pi_old(a|x)
J_sim        = mean_full_batch [w * min(rho*A_sim,
                                      clip(rho,1-eps,1+eps)*A_sim)]
L_BC         = mean_logged_data [-log pi_theta(a_logged|x)]
L_actor      = -lambda_sim * J_sim + lambda_BC * L_BC
```

其中 delta ≥ 0，仅在验证集定；各模型得分使用同一奖励尺度、时域和末端价值。门控同时接纳一致的正向与负向信号，方向分歧时 w=0。w、A_sim 与分支计算停止梯度，完整 batch 求均值，避免极少数通过样本被放大；不对历史日志动作错误套用 π_old 比率。

值函数先用固定数据 return 暖启动，再采用同一套模拟 return 拟合/目标网络更新规则，所有算法组共享此训练配方。该方法仅门控上述 actor 信号，不能消除 critic 的模型偏差。记录模拟与历史信号各自的有效样本数、损失幅度和占比。

这是**借用截断重要性比率的根动作学习目标**，并非原版完整 PPO。w 依赖动作，引入了有偏的选择目标；不声称无偏策略梯度、收敛保证或安全保证。三个模型共同犯错、数据支持不足或末端值偏差都可能导致错误通过。

### 6.3 为什么不把 GRPO / DPO 定成主线

GRPO 的组相对评分思想可以借鉴，但这里首先需要可靠的动作后果与风险尺度：同一组全都危险也能排出第一名。DPO 通常依赖可信偏好对及参考策略；从不可靠患者模型生成偏好，不会自动修复其偏差。两者也都不要求 Transformer，网络结构与训练目标是不同选择。

胰岛素控制已有 GRU/SAC、PPO、离线 RL、动作条件预测与规划等研究，不必为“借鉴 LLM 算法”增加无必要复杂度。优先把上述一个门控改动验证清楚，GRPO/DPO 可作为讨论或后续扩展。

领域参考：[Fox 等 GRU/SAC](https://proceedings.mlr.press/v126/fox20a.html)、[PPO/SAC 与决策频率比较](https://journals.plos.org/plosone/article?id=10.1371/journal.pone.0274608)、[离线胰岛素 RL](https://arxiv.org/abs/2204.03376)、[RL4T1D 作者项目](https://github.com/RL4H/RL4T1D)。引用仅说明研究基础，不表示本项目已运行其代码。

## 7. 对照、消融与验收

> 当前先完成论文基线、必要数据适配和独立闭环验证。下列涉及门控创新的分组及消融暂缓，不能作为基线开工依赖，也不能将暂缓项勾选完成。

### 7.1 最少必须有的比较

| 组别 | 用途 |
| --- | --- |
| 固定规则控制器、BC 策略 | 证明策略至少有合理参照；报告失败情况 |
| 模型内 PPO + BC | 常规模型式策略学习参照；明确自身实现，不冒称原 RL-DITR 复现 |
| 同根分支、同参考动作、w 恒为 1 | **归因必需的直接消融**，隔离门控贡献 |
| 普通预测分歧门控 | 同一集成与展开方式，检验是否只是一般不确定性筛样本起效 |
| 决策优势一致性门控 | 提议方法；其余输入、奖励、网络和预算与直接消融一致 |

所有学习组使用同一数据划分、预测器、动作合同和评价协议；相关模型式组使用同一三模型集成。报告环境步数、模型调用量及计算时长，避免把多用算力当算法收益。至少三个训练种子，结果按患者及场景配对呈现，不能仅挑最好一次。

必要消融先做“无门控 / 普通分歧 / 决策分歧”；再做有无预测特征和单模型/三模型。Transformer 对照后移，避免同时改结构和算法造成归因困难。

### 7.2 不只看奖励

- 控制表现：TIR、低血糖与严重低血糖区间占比、平均血糖/波动、动作总量与变化、失控和故障比例；常用区间作为研究指标预先登记，不作为个体目标建议。
- 患者模型：一步及多步误差、低血糖附近误差、候选动作优劣排序、分歧校准。
- 门控机制：通过率、正负信号覆盖、通过样本中的错误优势方向、全部拒绝时策略是否仅靠 BC。
- 反事实核验：在少量可完整恢复的仿真状态上，执行候选/参考动作并配对推进，用环境回报差核验模型排序；该诊断只在开发/验证协议内用于调参。
- 压力场景：未申报/延迟餐食、传感器缺失与噪声、边界动作、患者敏感性变化；训练与封存测试扰动分开。

成功不能定义成“所有输入拒绝”或“模拟训练奖励上涨”。必须同时报告策略的可用覆盖率、失败率、风险指标与闭环表现。没有改善也保留完整负结果，不转而调整封存测试集。

## 8. 与 App、LLM、RAG 和 Harness 的连接

对外继续只有两个主要数值产物：血糖预测和 RL 候选。患者模型属于 RL 训练内部依赖；首版部署 actor 直接推理，不要求运行三模型规划。登记训练依赖与当前实际执行组件，两者不能混为一谈。未来若推理新增规划或集成校验，其故障也必须按新合同处理。

RL 服务输入固定病例/历史快照和已验证预测引用，输出至少包含：

```text
run_id, case_id, snapshot_hash, forecast_parent_hash,
policy_version, preprocessing_version, training_dependency_manifest_hash,
executed_component_versions, action_kind, action_value, action_unit,
decision_time, duration, applicability_status, abstention_reason
```

这些字段的生产者身份、作业关联和权威登记由 Core 建立，不接受 LLM 自填字段作为真实性证明。模型输出不合法或不适用时返回弃权；Core 安全规则可否决，不能产生替代剂量。研究夹具必须标明 fixture，不能冒充实际训练模型。

RAG **纳入 App 首版，数值 RL 训练无需依赖它**。医学知识库提供可定位、带版本的解释依据和审核资料；模型/数据卡进入单独标记的技术知识库。它不替代动作条件模型，不计算新剂量，不把检索到的文字直接变成执行规则。

产品流程：Agent 核验资料并请求冻结 → 经受控 MCP 请求预测、请求 RL → Core 授权独立 worker 并登记双模型可信产物 → 确定性校验 → Agent 检索证据、提交解释 → 医学/伦理审核 → Core 发布 → 后续实际行为及测量反馈。模型执行器在 SDK 外部，但主 Agent 通过 MCP 发起请求；它不能改写权威数值或自行确认推理成功。剂量及时间由受控模板渲染；LLM 不得在自然语言里偷偷改数值、频率、条件或增添新疾病诊断。ClinicalBERT 省去后，病历抽取仍遵守来源定位和字段校验。

RAG 的文档清单、切块、混合检索、范围过滤、引用支持度、无结果/版本撤销与提示注入测试，已列入[总体方案第 4.4 节](01_项目总体方案.md#44-rag首版必做的可追溯知识层)。Observation callback 记录检索、模型、闸门与发布事件；训练门控指标亦可送入观测，但不能允许 App 的自动进化过程修改封存 RL 测试、规则或训练标签。

## 9. 详细 TODO 与交付节奏

| 阶段 | 具体 TODO | 完成证据 |
| --- | --- | --- |
| 选型来源审核，2026-09-11 | ☑️ 审核公开数据调研的获取条件、字段和闭环适用性；☐ UOM 泵子集全量可训练性审计 | [来源审核](05_公开数据集与RL闭环可行性审核.md)及 7 文件来源/hash；不是全量数据合格或已训练证明 |
| 真实联合取数与原始审计，批次38 | ☑️ 五源取得/解压/关键表全量审计及方法选型；☐ Ohio官方明文比对/来源条件最终核清 | [07审计报告](07_真实数据集RL训练资格审计报告.md)及专项脚本/清单；不把原始审计当净训练资格 |
| D01：Loop轨迹与全模型患者划分 | ☐ 事件重建、餐食/全胰岛素前史/体重/制剂资格；☐ F重叠及跨源已知重复核对；☐ split与净人日/episode/transition冻结 | 对应01 D01；369为提名上限，非合格人数；先患者角色再切窗 |
| D02：真实历史输入与动作合同 | ☐ 版本化真实来源、真实制剂、basal+bolus、command/derived/delivered及足够历史；☐ 依赖/弃权/单位/权限专项 | 对应01 D02；当前P01仍只合成/仿真，保留旧合同及已有验收 |
| 第 1 周：合同与环境 | ☐ 核对预测器窗口/人群/预处理；☐ 固定 simglucose 版本与成人划分；☐ 定义观测/动作/奖励；☐ 单位转换；☐ 轨迹恢复与 RNG 检查；☐ 测吞吐 | 同动作可重现；不同动作可影响后果；无未来信息；形成固定配置 |
| 第 1–2 周：轨迹与基线 | ☐ 规则/扰动策略生成训练轨迹；☐ 完整记录行为动作；☐ BC 基线；☐ 冻结 F 接入；☐ 真实开放样例字段审计 | 基线多日轨迹、数据清单及预测域误差；无需等作者代码/权重 |
| 第 2–3 周：患者模型 | ☐ 自行实现 GRU/MLP；☐ 三模型重采样训练；☐ 模拟历史更新与 F 适配；☐ 一步/多步/排序验证 | 训练与留出结果分离；错误与偏移可见；固定可复用模型包 |
| 第 3–4 周：学习参照 | ☐ 模型内 PPO+BC；☐ 配对根动作评分且 w=1；☐ 末端价值；☐ log probability/梯度/终止处理检查 | 两种参照可稳定运行；公式与实现逐项对应 |
| 第 4–5 周：算法改动 | ☐ 一致性门控；☐ 普通预测分歧对照；☐ 验证集阈值；☐ 通过率与错误方向诊断 | 同预算配对比较；正/负/分歧/全拒绝用例正确 |
| 第 5–6 周：正式实验 | ☐ 冻结训练配置；☐ 至少三种子；☐ 封存患者/场景评测；☐ 必要消融；☐ 失败轨迹及置信区间 | 全量结果及脚本可复查，不能只展示最优曲线 |
| 第 6–7 周：系统与章节 | ☐ 导出 actor；☐ 策略合同与父预测绑定；☐ Harness 故障/单位/越权测试；☐ App 曲线；☐ Observation；☐ 与 RAG 解释联调；☐ 方法和实验章节 | 双模型真实调用、拒绝路径、带证据报告与可复现演示 |

当前用户已将重点转为数值模型，下一步由用户下载五来源原包，之后每轮只做一个数据/模型具体子项。第一项是**许可、轨迹资格、原始事件语义及预测器训练重叠审核**，通过后冻结真实数据合同和划分。上表原仿真/算法/产品要求保留但不作为原包分析前置；旧周数不再是当前排期承诺。

主要交付：自研训练/评价代码、版本化数据与实验配置、基线及候选策略权重、可调用 RL adapter、对照图表与毕设章节、App 的正常和阻断演示。代码与产物均留在桌面项目目录。当前文件是实施计划，不代表这些工件已经完成。

## 10. 必须保持的研究边界

- 仅使用公开数据集和公开仿真资源，遵守各自许可；不依赖私有医院队列，不以复现 RL-DITR 原实验为目标。模拟结果不能当真实临床疗效；公开数据仍有患者重识别和外发限制。
- 训练用患者模型不能作为唯一裁判；模型集成全体一致仍可能全体出错。
- 预测精度不等于动作因果有效性，也不保证策略换分布后的准确性。
- 提议算法的新颖性与收益都待验证；发现近似前作时调整贡献定位，不将独立编码包装为首创。
- 研究展示中剂量仅来自已验证策略产物，解释受 RAG 与审核约束；不连接实际治疗设备。
