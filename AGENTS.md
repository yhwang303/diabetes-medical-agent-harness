# 本项目持续工作约定

- 按总体方案 v6 第 7.2 节的产品 TODO 优先顺序推进，原工程表及其未完成要求保留；每轮只做一个具体 TODO，完成并总结后停止，等用户审核通过再继续。不要一次跨多个 TODO。
- 到真实 LLM 接入步骤时再向用户索取 API key，由本地安全配置保存；此前不提前索要、不虚构联调成功。
- 最终产品必须接入 Agent SDK 与真实 LLM，由 Agent 驱动任务、工具调用与失败处理；固定按钮流程/占位器/直接 provider 调用不替代 SDK 验收。Core 保留权限、证据登记与发布的最终控制权。
- 开发默认仅使用 DeepSeek 官方 Flash，禁止 Pro 和自动回退到 Pro。当前已核验的官方 ID 为 deepseek-flash（V4.1 Flash，承接用户指定的旧 V4 Flash）；多模态仅在需要时核验用户指定 Vision 型号，不提前产生视觉调用费用。真实调用设置次数、输出和费用预算；密钥仅保存在本地私有配置，不写入源码、工作记录、日志或命令参数。

- 每完成一批功能、修改或验证，立即更新根目录 `工作记录.md`，不要只在最终回复里说明。
- 工作记录须区分已完成、验证结果、已知限制和待办。同一内容发生冲突，以最新记录为准；保留旧记录以便追溯。
- `工作记录.md` 最后固定维护“已完成 Harness 功能清单”：按真实实现列出功能及边界，有更新或修改立即同步；新的批次记录插在该清单之前，清单始终位于文档末尾。
- 在 Harness 约束下逐步扩展 Agent 工具，按任务授予必要能力；增加工具须同时验证权限、输入、证据绑定、失败处理与审计，不凭增加工具数量放宽闸门。
- 当前执行入口为 `01_项目总体方案.md`、`02_RL动态给药模型方案.md` 和用户最新指令；`03_原长版方案_v3.5_保留.md` 仅作历史资料。
- 当前先实现 Harness。真实预测和 RL 模型未接入；fixture 不得冒充真实模型或医学验证。
- 用户允许可确定的等价单位转换，不因 U/min 与 U/h 表示不同直接拒收；保留模型原始数值/单位与证据引用，转换由受控代码执行并绑定版本。转换不改变基础率/总量/单次注射的语义，不替代来源、适用性、时效及安全检查；不能静默量化模型动作。
- 可信模板、动作语义、风险标识、规则、权限和 renderer 不允许自动进化修改。
- 项目运行数据、日志、缓存和构建输出保持在本目录；不要改动其他项目或其历史数据。
- 不把实现、单元测试或 fixture 成功写成真实模型联调、原生 App 验收或临床安全证明。
- 工作中发现 TODO 遗漏、错误或与真实实现/用户最新约定冲突时，及时修订和补充，并在 `工作记录.md` 记录原因。每项只有在实际实现且验证通过后才勾选；未完成、暂缓和未验证项保持未勾选，不通过删除要求或降低标准制造完成状态。
- TODO 表格统一使用 `☑️` 表示完成、`☐` 表示未完成，避免 `[x]` 在表格中显示成普通字符。

- 最终产品是面向用户的糖尿病医疗 Agent，体验为多模态生理数据输入、必要的自然对话追问、胰岛素建议与自然语言解释、后续测量/实际用药反馈。研究工作台、固定脚本、章节排序提案或 SDK 接通均不等于产品闭环完成。
- Agent 主导任务规划、获准工具选择、追问和有限失败恢复；Harness/Core 强制执行前置条件、来源/适用性/安全检查和唯一发布。个体化给药结论必须绑定两模型有效结果；缺失、过期、不适用、弃权或失败均不以 LLM 猜测、零剂量或“维持”替代。追问及无剂量的受控状态/风险升级说明不必等双模型。
- 两个数值模型经受控 MCP 供主 Agent 请求，执行器继续由 Core 管理并在独立 worker 运行；不让 Agent 绕过 Core 直连裸模型或自行登记结果。MCP 与 SDK 外独立执行兼容。
- RL-DITR 只学习方法，不复刻原论文的任务、实验或私有医院队列；研究仅使用公开数据集与公开仿真资源，遵守许可。不得把获取私有医院数据设为依赖。
- 用户授权结合公开论文、数据及模型选择首版胰岛素任务；v6 选择成人 T1D 基础输注研究场景，理由及边界见 02 第 1.1 节。不得把泵速率/区间总量偷换成单次注射剂量，也不得未经新合同和验证扩大到其他制剂、动作或人群。
- 持续区分工程闭环、真实双模型仿真闭环、用户任务闭环与真实患者产品准入；建议已发布/已读/接受不等于实际用药。产品愿景不代表已获医学有效性证据。

- 正式 self-evolution 的先后顺序已由用户确认：先完成必要产品链路、两个真实数值模型及 Harness 各项闸门验收，再按冻结的 evolve_train/evolve_validation/sealed_test/demo 划分使用许可允许的公开真实数据开展。审计和测试协议可随开发准备，但不提前运行正式进化或用 fixture 宣称整产品进化收益。患者划分先于切窗，核对与数值模型训练集重叠，封存集不参与候选生成/调参；不适用的真实数据仅评价资料处理/阻断路径，不放宽模型闸门。仿真与故障用例继续承担对应的独立验证职责。
