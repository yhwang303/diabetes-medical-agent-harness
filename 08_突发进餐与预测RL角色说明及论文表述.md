# 突发进餐、预测与 RL 的角色：产品定位及论文表述

2026-09-12。说明与拟议论文措辞；没有修改数据、训练模型或实现产品能力。承接07审计的成人T1D皮下泵基础输注范围，不扩展为餐时bolus或静脉滴注控制。

## 1. 核心回答

首版RL不能只依据历史血糖决定所有胰岛素剂量。合理输入还须有实际基础/bolus给药史、当前仍起作用的胰岛素状态估计、当时已知的餐食/活动和数据质量。血糖上涨不自动意味着应追加胰岛素：刚给过bolus而尚未充分起效的历史，与没有给过bolus的历史，后续风险不同。

如果两种情景到此刻为止的输入完全一样，其中一人下一刻突然吃蛋糕，另一人不吃，任何只读取这些共同历史的模型都无法确定谁会吃。它最多学习进餐发生的概率或典型模式，不能知道一次未公告的新事件。这是可观测信息的限制；加大RL或LLM不能消除。

App的合理价值在于持续更新判断：能够获知的事件尽早纳入；未知事件随着新测量出现而被发现和重新评估；每轮结合已有药效避免追涨式重复给药。其价值和效果仍待实验，不等于承诺完全消除餐后升糖。

## 2. 蛋糕例子的三个阶段

| 阶段 | 系统真正知道什么 | 拟议行为与边界 |
| --- | --- | --- |
| 吃之前，尚未告知 | 旧CGM、真实给药史、其他已知背景 | 基于当前信息预测与选择短区间基础动作；不能提前知道蛋糕 |
| 刚吃完，用户告知，但CGM尚未体现 | 新进餐事件、摄入/公告时间；份量/碳水可能仍不确定 | Agent把事件变为带来源和不确定性的结构化资料，必要时追问；冻结新快照并重验旧候选。只有输入与训练支持餐食的模型能作餐食条件数值更新 |
| 未告知或迟报，随后CGM开始变化 | 新观测及相对旧预测的偏差，未必知道原因 | 滚动更新F/RL；必要时询问进餐、给药或设备问题。偏差不证明一定吃了蛋糕，也不能据此直接推断碳水克数或追加剂量 |

吃蛋糕不意味着CGM立即出现固定幅度上升。餐食组成、量、已有给药和个体生理都会影响时间过程；皮下给药及测量也存在延迟。因此“没告知也能追上所有升糖”不是可信产品承诺。

真实AID也存在这个限制。[NIDDK说明](https://www.niddk.nih.gov/health-information/diabetes/overview/managing-diabetes/artificial-pancreas)将需要用户餐食输入的系统称为hybrid。一个14名成人的单臂先后阶段研究中，未公告与公告阶段TIR分别为67.5%与77.7%；支持餐食信息有价值，但不是我们的模型结果，也不能直接移植为本项目预期收益。[Shalit等2023](https://pubmed.ncbi.nlm.nih.gov/37335759/)。餐食构成与不同控制器餐后输注过程的研究见[Scidà等](https://pubmed.ncbi.nlm.nih.gov/38840523/)。

## 3. F、M、RL与Agent/Harness的分工

| 组件 | 职责 | 不能声称的能力 |
| --- | --- | --- |
| 预测器F | 根据已观测输入提供未来趋势/风险估计，随新信息滚动更新；不确定性须另经校准验证 | CGM-only F不感知刚公告但尚未影响CGM的蛋糕；无候选动作输入的F不能比较替代剂量后果 |
| 生理患者模型M | 在训练/验证中描述不同基础动作、已存在药效和餐食扰动下的动态响应；首选机制结构辨识 | 仿真预测不是实际患者反事实真值；不因事实拟合好就证明动作响应正确 |
| RL策略 | 结合历史状态、有效F信息及已有药效，权衡未来风险，选择下一短区间基础率 | 不是把预测高血糖按比例转换成剂量；本版不输出餐时bolus，不能拿基础率变相替代 |
| Agent | 收集/澄清进餐和实际用药，组织获准模型调用与解释，管理用户反馈 | 图片或一句“一块蛋糕”不能变成确定碳水真值；LLM不能修正数值预测或补算剂量 |
| Harness/Core | 同快照证据、时效/适用性/动作/权限检查，决定候选能否发布；新关键事件使旧结果重验 | 双模型都返回数字不等于结果有效；发布/接受不等于药物已实际施用 |

F为历史条件预测时，预测还受训练中典型后续行为影响。不能把它未经验证称作“维持当前剂量的轨迹”或“完全不打胰岛素的轨迹”。只有有相应条件输入、训练与验证的M/预测器才能赋予这种治疗条件含义。

当前用户已有F若只接收CGM，要明确保留为CGM趋势分支；长远可适配/重训带给药和已知餐食的事件条件F，但本轮没有实现。RL可被设计为独立接收进餐输入，不过这不意味着旧F已经利用了该信息；各模型输入范围与适用性须明确。旧F不支持所需情景时，应返回受限解释/无剂量状态，而不是让Agent手改F曲线。

理论上RL可以直接用历史而不单列F。单独F的科学价值必须通过“相同观测/训练条件下，有F与无F”的消融证明；现有Harness要求双模型证据是工程约束，不能当作双模型医学效果更好的证明。

## 4. 本项目应该如何定位

目标是“面向信息不完整和事件变化的、可追溯的持续决策支持与反馈更新”。用户报告餐食可以减少信息盲区；CGM变化提供后续纠偏；真实给药反馈防止把未执行建议当已用药，或把已用药遗忘。

首版研究固定成人T1D基础输注RL，历史餐时/纠正bolus必须进入生理状态。模拟中餐时、纠正及救援规则独立冻结并对所有基线一致；现实产品若要建议餐时bolus，应另建动作合同/模型/验证，不能以本轮讨论自动扩大首版范围。

如果产品仍只支持上传一次CSV、给一次建议，没有新CGM/事件/实际用药回流，那么只能称单次回顾性评估，不能称持续闭环。App接收用户反馈属于决策支持闭环；直接接泵执行是另一种具有不同权限和医学准入要求的产品，不因界面循环就成立。当前项目二者均未完成真实模型验收。

## 5. 需要安排但本轮不执行的实验

同一独立生理场景固定其他条件，分别比较：及时报餐、迟报餐、未报餐、报餐量误差；另按最近bolus/不同IOB、吸收速度及传感器噪声分层。环境知道真实摄入以生成响应，actor只接收当时实际可用信息；训练中不泄漏未来事件。更高信息可见性不能用作唯一解释某算法优于另一算法。

比较CGM-only、CGM+给药史、再加已知餐食的状态；同状态下进一步比较RL有/无F趋势分支，BC/规则基线/TD3+BC共用伴随bolus控制器。报告餐后超范围持续时间、TIR/TBR、迟发低血糖、动作及总暴露、弃权率、有效建议比例和用户确认负担；安全与可用性分别报告。单独报告F预测误差与控制指标，不以RMSE改善代替控制收益。

本轮只确定这些科学问题，未冻结最终统计检验/阈值、未运行仿真或正式self-evolution，也不预设RL必须优于基线。

## 6. 可用于论文的研究方案表述（尚无实验结果）

日常进餐与活动为血糖调控引入了部分可观测的时变扰动，仅依据既往CGM序列的单次预测难以描述尚未发生或未被记录的行为变化。因此，本研究拟将成人1型糖尿病基础输注调节建模为持续接收观测与事件更新的序贯决策过程。系统以CGM、实际基础及bolus给药历史、当时已知的餐食信息和可用生理背景构建患者状态，由预测模块提供条件性血糖趋势及经校准的不确定性估计，并由强化学习策略在已有胰岛素作用和动作支持约束下选择下一决策区间的基础输注率。针对未公告进餐，系统不假设能够预知摄入事件，而是随新观测更新状态与风险判断，并在关键资料不足或模型不适用时请求澄清或暂缓给药结论。动作条件生理模型用于策略开发及受控扰动验证，餐时与纠正给药作为独立、预先规定的伴随机制处理。Agent负责事件采集、工具协调与解释，Harness负责输入来源、双模型证据一致性、适用性及发布权限约束。本研究将分别在及时、延迟与未公告进餐条件下评估血糖控制、低血糖风险及系统可用性，并通过有无预测模块的消融检验其对策略决策的增益。

这段是拟议方法/研究定位，不可将“拟、将”改成已实现/有效，直到模型与实验完成。文中事件条件输入及不确定性校准是待建能力；不能把当前CGM-only F写成已满足。

## 7. RL-DITR原文追加核对：监测频率与部署输入（批次40）

2026-09-12按用户追问重新核对本地完整PDF：正文PDF第2页、Discussion第9页、Methods第11页及可行性试验第14页。来源：[本地原文](参考论文/RL-DITR_NatureMedicine_2023.pdf)、[正式论文](https://www.nature.com/articles/s41591-023-02552-9)。在线正式页open遇到跳转限制，正式页搜索可检索的原文与本地PDF对应；没有推测未读取的补充表逐字段内容。

- RL角色：依据包含历史临床与治疗信息的状态，通过动作条件patient model进行多步治疗规划，优化累计血糖状态奖励，并结合医生行为监督；不是单次预测血糖高低后直接比例换算剂量。
- Methods的observation包括人口资料、诊断、症状、用药、血糖和实验室信息。每天七时段是输入记录分组；不等于每个人仅提交七个血糖数字，更不等于每天自动改变剂量七次。
- 可行性试验为住院T2D：首次治疗周期后进行AI滴定，医生每天两次确认并可拒绝；标准餐食按通常餐时供应，未安排体育活动，其他降糖药不变。每天七个时点的毛细血管血糖用于指导方案；CGM用于评价范围内时间等指标，原文明确治疗不受CGM数据影响，CGM报警未开启。
- Discussion明确院外饮食变化和活动需要进一步考虑，并提到为后续模型更新积累饮食信息的接口；不能声称其已解决自由生活未公告进餐。
- 我们的成人T1D基础输注任务需要更细的反馈节奏，但更多CGM只缩短可观测变化的反馈延迟，不提供尚未显现的进餐/补注事实。目标应为自动同步CGM和泵已递送记录，背景资料初始化/变化更新，餐食和未被设备记录的给药由事件报告补充；并非让用户每五分钟手工上传。
- 模型训练完成固定的是权重，不是患者当前给药史。CGM-only预测器可以按已验证输入合同输出趋势；本项目真实剂量策略不能据“训练完成”删去给药史/制剂/必要状态。缺失模态训练或蒸馏属于另一个待验证模型，不能将当前全输入模型直接断开字段。
- 仅依赖CGM的研究控制策略可以存在，其未观测状态和安全适用性须专门验证；这不支持本项目当前给出“CGM足够用于个体给药”的产品结论。即便用户界面只显示CGM，后台仍可能自动维护实际动作/药效状态，不能将低交互成本等同于单一信息输入。

本次仅澄清原文与模型部署输入，无代码/训练/数据处理变化。

## 8. 用户澄清后的产品定位与报告文案（批次41，以本节解释当前讨论意图）

用户本轮只讨论产品故事和使用流程，不要求解决所有突发事件，也不修改训练方式、数据集或当前模型动作范围。此前答复过度转向全自动控制和算法充分性，偏离了这一产品问题。

确定的产品叙事是：**一次建档，按需评估，情况变化后重新评估，实际执行后反馈。** 模型固定权重；新增血糖/事件触发的是状态更新与重新推理，不需要在线RL。在线RL通常还涉及依据新交互更新策略参数，与此不同。

目标使用流程：

1. 首次使用由Agent整理患者基本情况、疾病史与治疗配置，建立可更新档案。
2. 早上8点，患者上传/同步近期CGM；系统调用已有档案及最近实际用药记录，检查信息适用性，生成血糖趋势与当前动作合同范围内的胰岛素调整候选，说明生效/适用时段、依据和再次评估条件。第一次建档不代表未来实际用药记录永久已知。
3. 用户/临床人员记录实际执行时间和用药，而不是将建议被阅读/接受写成实际用药。
4. 发生额外进餐、运动、用药改变等可能影响本轮建议的事件时，用户点击重新评估，报告事件并补充/同步最新CGM和实际用药。Agent补齐缺项，系统重验旧方案适用性；更新完成后清楚区分旧版与新版。事件未被CGM反映时，不能把重复上传同一曲线本身视为已经观测到事件。
5. 即使无新事件，也按事先确定的有效期/监测计划再次评估；不能让8点一次输出无限期有效。临床研究场景可明确设置持续佩戴CGM并接入数据流，由系统触发提醒/评估，按研究流程交临床人员确认。

产品可采用上传文件作为早期入口，以后增加自动同步；不需要每次重新建档或重复上传整段历史。新事件不保证一定生成不同剂量：同样可以得到原建议在新信息下仍适用、需补资料或暂不输出新候选。模型支持范围不足时不能承诺报餐后立刻精准补偿；这不妨碍将重新评估作为产品交互。

展示措辞宜用“个体化胰岛素调整建议（明确适用时段）”。当前模型为基础输注，具体显示基础率及持续时间，不把其区间量写成一次注射剂量；如要给出未来多时点方案，必须由经过验证的动作/规划能力支持，本节不扩大该能力。

### 产品报告可用段落

本产品拟面向需要持续血糖管理的患者，构建由个体档案、血糖趋势预测、胰岛素调整建议及执行反馈组成的决策支持流程。首次使用时，Agent协助整理患者基础信息、疾病史与治疗方案；后续评估复用已有档案，并结合最新血糖及实际用药记录，由预测模型与强化学习策略生成适用范围明确的趋势分析和调整建议，交由Harness完成证据、时效与适用性检查。当进餐、运动、用药或其他相关情况发生变化时，患者可报告事件并补充最新监测数据，触发重新评估和建议版本更新。在持续CGM监测的临床研究场景中，该流程可通过自动数据同步与预设触发条件运行，并按研究流程由临床人员确认。产品旨在减少患者反复整理资料和理解模型结果的负担，支持随状态变化而更新的个体化管理；其核心交互不依赖模型在线更新参数，实际效益须通过后续验证。

本节为拟议产品流程/文案，不是已实现、临床获准或模型有效性声明。核对的CGM自动传输能力依据：[NIDDK官方说明](https://www.niddk.nih.gov/health-information/diabetes/overview/managing-diabetes/artificial-pancreas)。未修改数据、训练方案或实现。

## 9. 当前交付边界与数据规模判断（批次42，用户最新澄清）

当前以手动上传/资料补充→预测与受控建议→实际执行反馈→再次评估打通产品闭环。临床长期监测及设备自动上传仅后续扩展，上一节的临床自动同步段落不属于当前交付范围。用户本轮未授权开始实施新TODO，也未改变RL训练方案或数据选择。

Loop保持首版RL与生理患者模型的唯一主要真实来源候选。已有审计的成人CGM+basal+bolus交集507人，加food为502，加体重为454；进一步问卷筛选提名上限369人。这不是最终训练人数。一个来源可以包含数百人的纵向轨迹，不等于只有一个患者或一个样本；但全队列原始行数含重复、儿童与未通过质量的片段，不用于证明训练规模足够。

决策：其规模足以支持优先开展首版数据资格构建，不为凑数量继续找源；是否充分支撑训练和泛化，要等净独立患者/连续人日/动作覆盖及患者级学习曲线验证。关键不是来源数量，而是完整basal+bolus/餐食前史、不同生理状态下可用动作支持、患者多样性及留出表现。不能承诺369人一定保留或大行数必然得到有效策略。

其他数据集保留但不成为首版必须全部使用的依赖：DCLP3供条件预测/行为分布和输入鲁棒性研究；Ohio供预测/小规模多模态及可检查规则对照，先查现有F重叠；UOM供活动/睡眠等可选模态研究，先解决质量与可用时刻；IOBP2整源外部部分评价，不参加训练/调参，缺CHO不作完整反事实评价。它们分别可帮助模型辅助开发和来源迁移/工程输入检验；原始真实健康数据接入App仍须对应新合同，不能以“测试用途”绕开。

后续若考虑使用其他源，只在其明确任务上增益值得验证时启用，不将所有来源凑入同一RL动作池。此处没有新增训练、正式处理、模型成绩或合格规模。
