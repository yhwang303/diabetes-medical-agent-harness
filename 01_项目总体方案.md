# 糖尿病医疗 Agent：产品闭环、强 Harness 与实施清单

> 当前研究版本：P03 DSENet（global12/3、local2/1）+D05 world+D06有限候选RL，单seed260915；主表TIR97.08±4.50、TBR70 1.55±3.89、TBR54 .57±1.79。最新正文比较含8种外部算法及hold，见[正文报告](RL_DSENet_2026-09-17/revision/论文正文对比报告.html)与[交接](RL_DSENet_2026-09-17/revision/交接与证据.md)。保留单seed、额外仿真监督和个体低糖边界，不称全指标SOTA；Core/Harness产品准入不变。


> 2026-09-16 最新目标覆盖：以RL-DITR完整患者模型、策略/价值和beam规划为基底，先跑通后研究近期论文支持的创新；O07/ReBRAC不再作为我方主线。新训练仅seed260915、保留全量Loop及稀疏food/exercise；FQL/ReBRAC不再运行。多维评价与新场景先冻结，已有10个模拟成人均已曝光。任务未完成，见[本轮合同](RL_DITR创新_2026-09-16/任务与复现合同.md)。


> 2026-09-15 22:14 最新研究结论：O07固定历史参考±0.4 U/h残差ReBRAC（seed260915、50k、全部225训练患者）通过冻结后的6名独立模拟患者研究门槛，TIR94.97%、TBR70 2.05%、TBR54 0.52%、0/54终止；仅可作为Harness受控研究接入候选。adult009低血糖仍明显、只有单seed，未证明临床安全。独立JSON推理及Mac权重一致性已验；未注册Core，M02产品父项/M02a及双模型准入保持未完成。最终结果、失败尝试和论文表见[训练审核报告](RL进阶对比_2026-09-15/训练审核报告.html)。此条覆盖下方历史阶段的“尚未训练/策略未启动”状态，不删除其负结果。


> 2026-09-15 21:49 用户最新覆盖：所有模型均只训练一个seed，主比较使用260915；已停止O07额外seed进程，未启动的下一seed撤出。此前多seed记录仅作历史证据，不再列为当前必做要求。后续同一批独立评价情景用于所有方法，不涉及重新训练；单seed不证明随机初始化稳定性。


> 2026-09-15 21:41 用户最新研究范围：外部ReBRAC/FQL baseline各用一个训练seed的同数据可比结果即可，后续重点优化我方模型；未提交的额外baseline seeds已撤出。我方候选继续稳定性与独立评价。当前为个人历史参考+有界残差结构、行为/价值损失权重及去RL消融，全部225训练患者和1,653,421起点保留，性能阈值未改变。实际结果以[进阶训练审核报告](RL进阶对比_2026-09-15/训练审核报告.html)和研究协议最新条目为准；原方向比例不是验收门槛，真实双模型/Harness产品准入仍未完成。


> 2026-09-15 20:28 持续目标更新：用户已明确授权2–3个较新RL基线公平对比、有论文依据的损失/risk/模型结构实质改进、多seed训练和独立仿真，最终交付全部实验HTML与论文式对比表。当前ReBRAC/FQL真实离线策略及BC已实现并在AutoDL运行50k pilot；新实验入口为[研究协议](RL进阶对比_2026-09-15/研究协议.md)。旧80%动作方向门槛仅保留诊断，不作为新策略验收；独立闭环合同已在成绩产生前冻结。Loop全部225训练患者/1,653,421起点和food/exercise保持，旧轮次权重负结果保留；不能将新策略已启动写成旧轮次已有完整RL。当前总目标仍未完成，M02a/M02未勾选。此条覆盖下文旧仅复现不创新和停止策略训练的当前执行限制；产品可信规则与Harness闸门不修改。


> 2026-09-15 当前执行：用户已授权在AutoDL连续训练并按结果调整，使用[22维训练管线v2](Loop数据集/训练管线_v2/README.md)，food/exercise只加特征和mask，225名train/1,653,421起点不减少。实际训练及失败结果见[训练审核报告](RL训练_2026-09-15/训练审核报告.html)；本轮实验已结束：S00烟测、A01失败、A02/A03各完整1epoch及全量validation已验证。A03的30/60分钟RMSE为0.9973/1.6775，但+0.1U/h仅2.50%诊断样本预测下降；当前权重不能用于RL给药，策略阶段因患者模型动作响应未通过而未启动。M02a/M02仍未完成，Harness未接入真实模型。此条覆盖下文旧14维默认训练/未有SSH等状态。

> 2026-09-14批次44最新：已完成 [Loop无插值预处理与最终审计](Loop数据集/预处理_v1/00_从这里开始.md)。实际train为225人/1,653,421条transition；validation55人、sealed_test32人、demo17人。329是四组合计。原始文件未改；真实模型与反事实/临床闭环未验收，D01父项仍未完成。下文此前“未处理/369候选”属于历史状态。

2026-09-12 批次42产品范围澄清：当前先以手动上传/补充资料、评估、受控建议、实际执行反馈和再次评估打通产品闭环；临床长期监测和设备自动上传属于后续扩展，不作为当前交付前置条件。本次不修改RL训练方式、数据集选型或启动其他TODO；Loop仍为首版RL/生理主池候选，其他来源用途分层。

2026-09-12 批次38最新结论：[真实数据集RL训练资格审计报告](07_真实数据集RL训练资格审计报告.md)。五来源已解压并完成关键表全量审计；Loop为唯一首版RL/生理主池候选（369为提名上限，非合格人数），DCLP3/Ohio/UOM用途分层，IOBP2整源外部部分评价。先资格冻结，再BC/TD3+BC和ReplayBG生理结构辨识，通过独立验证后开展模型式RL；当前Harness须新增真实来源/basal+bolus合同。未正式处理、未训练或改产品实现，M01/M02未完成。以下旧来源分工与下一步安排由本条及07覆盖；历史要求保留。

更新：2026-09-10｜v6：Agent 主导、多模态用户体验、胰岛素建议与反馈闭环。

状态：已有 Harness 工程基础和真实 SDK/Flash 报告、审核链路；完整用户产品、真实预测/RL 和医学验证未完成。2026-09-11 批次 20 完成 P01b 动作/配置/证据时效绑定，P01 两个子项及父项工程合同验收通过；批次 21 按用户要求补充保留原值的等价单位转换（当批全量 440 项通过）；批次 22 完成 P02a 数据流/外发授权；批次 23 完成 P02b 工程保留/删除与 SDK 登记目录回收，P02 父项合同验收通过；批次 24 完成 P03 受控预测 MCP，批次 25 完成用户补充的 P03a 设置组件与本地自检，批次 26 完成 P04 受控 RL MCP 与下游父 SDK/许可重验，最新全量 604 项通过；完整真实资料防护仍属 P16；真实模型配置和医学验证仍未完成。v6 复审理由与来源见 [产品闭环审查](04_产品闭环审查与验收.md)。

配套：[RL 动态给药模型方案](02_RL动态给药模型方案.md)。原始构想保留在桌面原文件 `/Users/george/Desktop/diabetes_agent_harness_sdk_deepseek_plan.md`。

按用户要求，已从本会话历史编辑记录恢复 [原长版 v3.5（24 章）](03_原长版方案_v3.5_保留.md)，作为历史设计保留。长版中的旧附件相对引用保留原状；当前执行入口仍是 01/02 两份计划。

已确认路线A；2026-09-14用户进一步明确：先按RL-DITR原文与作者代码复现状态表征、动作条件患者模型、策略/价值的训练与规划方法，跑通研究闭环，再根据结果讨论结构或损失创新。当前不开展DPO、门控等改进。保留现有CGM/Loop成人T1D泵场景，必要输入/动作/时间适配逐项记录，不复刻原住院T2D临床实验或声称原临床效果。仅用许可允许的公开数据/仿真，不依赖私有队列或作者权重。独立预测器F仍为产品双模型之一，不能默认直接替换内部潜在状态预测头fP；接口、适用域和训练重叠仍需核验。ClinicalBERT不列为结构化Loop数据的必需组件；主Agent模型及SDK要求沿用现行约定。

## 1. 先看这一页

**最终产品是面向用户的糖尿病健康 Agent：输入多模态生理数据，通过自然对话补齐资料，获得胰岛素建议、自然语言解释，并能反馈后续血糖和实际用药。** 用户借用“类似蚂蚁阿福”描述交互感受，不代表本项目已经拥有其能力、服务或医学资质。Mac 原生桌面是首个载体，研究工作台只是内部验收视图。暂不扩展 SaaS、多租户或实际泵控制。

**Agent 拥有任务决策权，Harness 拥有执行与发布否决权。** Agent 决定问什么、使用哪些获准工具、如何响应缺失或失败、怎样组织解释；Core 不再用预先跑完两个模型的固定脚本替代主 Agent。具体胰岛素动作必须来自批准的 RL 结果，Agent 不能自行计算替代剂量。预测与 RL 两份有效结果都是个体化给药结论的必要条件，通过双模型闸门后仍须接受适用性、医学规则、内容及发布审核。

用户已授权根据论文、数据和模型确定首版任务。本次选择 **成人 T1D、胰岛素泵基础输注建议** 作为首个可验证的胰岛素场景，先在虚拟患者中训练和验收；明确显示速率、持续时间及该区间总量，不表述为“一次注射多少单位”。这是当前证据下的工程与研究选择，非临床最优治疗结论。餐时/纠正 bolus、长效注射调量和 T2D 方案需要分别重建数据、动作合同和验证，不通过换单位直接扩展。判断依据集中在 [02 第 1.1 节](02_RL动态给药模型方案.md#11-首版胰岛素任务选择与产品衔接)。

用户已有的预测器只根据历史预测未来血糖；其适用人群、窗口和仿真域能力仍待核验，不能据此预测某个新剂量的疗效。当前继续优先实现 Harness，两个数值模型保留明确标记的 fixture；这只允许工程演示。真实数值模型接入和 RL 训练另按单项 TODO 推进。

目标流程（待实现部分不能据此宣称已打通）：

```text
用户对话 + 生理数据/图片/报告
  → Agent 理解任务，调用受控解析、病例读取、资料核验工具
  ↔ 必要追问、冲突消解、关键事实确认
  → Core 冻结病例与当前时点的输入，验证适用范围
  → Agent 经 MCP 请求预测 → Core 授权独立 worker、验收并登记产物
  → Agent 经 MCP 请求 RL → Core 装配已验预测及快照、验收并登记产物
  → 双模型有效证据 + 确定性安全闸门
  → Agent 检索受控知识、提交有证据绑定的解释及建议提案
  → 受控渲染 → 同版本双角色审核 → Core 唯一发布闸门
  → 用户看到胰岛素建议卡、自然语言解释、来源、适用条件和有效时间
  ↔ 用户反馈实际用药/未执行/后续血糖 → 核验新事实 → 新快照、新评估

缺资料 → 追问；模型不可用/弃权 → 无剂量的明确说明；风险 → 经审定的升级处理
全过程 → Core 审计/outbox；随后接 Observation，最后实施受控进化
```

研究资源只使用公开数据集和公开仿真器，按资源许可取用；原论文私有医院数据既不是依赖，也不列入索取计划。

首版包含两个严格分开的验收轨道：

| 场景 | 能展示什么 | 进入完整策略链的条件 |
| --- | --- | --- |
| 公开历史/真实配对多模态病例 | 导入、质量核验、一般资料解释、符合适用域的预测 | 未经该人群和动作验证的 RL 不生成个体胰岛素建议；CGMacros 等多模态演示不冒充 T1D 给药验证 |
| 虚拟成人 T1D 病例 | 面向用户的交互、双模型推理、基础输注候选、解释、反馈及仿真后果 | 两个实际数值模型通过仿真域验收；输入、报告和反馈全程标明研究性质；fixture 不能代替真实模型验收 |

真人历史不能接到任意虚拟患者上伪装成同一人的反事实。预测器未适配仿真域时应阻断完整链路。多模态原型和仿真成功不能直接解锁真实患者给药；真实服务另经过第 7.3 节的医学与产品准入。

## 2. 强 Harness 到底强在哪里

约束不依赖模型“听话”。Core 是独立的确定性程序，拥有任务状态、受控推理、产物登记、安全规则和最终发布权。LLM、subagent、CLI、MCP 返回文本都只有提交请求或草稿的权限。

| 组件 | 负责 | 不拥有的权限 |
| --- | --- | --- |
| Harness Core | 输入快照、作业、证据登记、规则、状态机、发布 | 不把 LLM 自报成功当事实 |
| 预测/RL adapter | 从指定输入调用批准的模型，返回有类型的结果 | 不能跨病例拼接结果或自行放行报告 |
| 主 Agent（SDK + LLM） | 理解任务、规划、追问、选择获准工具、有限恢复、组织解释与提交提案 | 不能编造/改写剂量、签发证据、放宽规则或绕过发布 |
| RAG 检索服务 | 返回授权知识片段、定位和版本，供解释与审核使用 | 不能更改模型结果、自动激活规则或独立生成个体剂量 |
| 抽取/审核 subagent | 有限资料抽取或审查指定版本 | 不能写核心数据库、改规则、替代模型结果 |
| CLI / MCP / App | 调用同一个 Core API | 不能直接注册“成功结果”或显示未发布报告 |
| Agent Observation | 展示行为，收集改进反馈 | 不能授予医疗报告发布权 |

CLI 不比 MCP 天然可信。两者都可以连接失败，也都可能返回任意文本。真正的区别是：**失败之后，Core 没有合法产物就不允许后续诊断节点启动。** Python 只是便于接模型、处理数据、做类型校验和事务的工程选择，TypeScript/Go/Rust 也能实现同样约束。

### 2.1 双模型结果作为唯一判据，具体怎样强制

1. Agent 在必要资料核验后请求冻结；Core 根据授权的病例记录建立 `snapshot_id` 和 `run_id`，不接受任意聊天摘要替代快照。
2. Agent 经受控 MCP 请求预测；Core 校验前置条件、给受信执行器分配 job，只接受该 job 的结果。
3. Agent 请求 RL；Core 从权威库读取预测产物并装配输入，禁止 Agent 重写 CGM 或预测数组。请求顺序错误返回可处理的结构化原因，不替 Agent 伪造成功。
4. 两结果都通过来源、同病例/时点/父依赖、版本、单位、适用性和安全检查，Core 才签发 `VerifiedModelEvidence` 引用。
5. 正式分析节点只接收这个引用，由服务端加载证据；不接受客户端上传的数值或 `verified=true`。
6. LLM 只能引用被允许的事实和结论槽位；剂量、时间、频率、动作单位由受控 renderer 从 RL 合同渲染。
7. 最终内容审核通过，Core 才原子提交 `ReleaseBundle`。App、导出和 API 只展示该 bundle。

**这里限制的是正式结论的来源和发布，不是证明两模型永远正确。** 预测曲线与胰岛素候选也不足以诊断任意疾病；历史条件预测不得被描述为采用本次建议剂量后的血糖后果或疗效保证。正式个体结论只覆盖输出合同支持的含义；RAG 可提供通用知识，病例资料用于输入/适用性，安全规则可以否决，但都不能补出新的个体剂量或冒充第三个诊断依据。

自由自然语言无法靠 JSON、正则或另一个 LLM 保证零错误。因此，首版把正式诊断性结论和给药含义限制为审定槽位/模板；LLM 负责选择、排序和解释证据。无法约束的推测不得显示给普通用户；未发布草稿只允许绑定任务的审核角色及获准的内部审计读取。产品解释须扩展成第 3.5 节的受控语义合同，不能把目前仅选择章节顺序的工程提案称为完整自然语言建议能力。

### 2.2 信任边界

可信部分尽量小：Core、受信 adapter、权限与事务库、规则包、受限 renderer、发布 API。Schema 只证明结构，hash 只证明一致性，签名只证明签发身份，都不证明医学正确或远端内部诚实执行。

首版不给 agent shell、任意文件写入、任意网络访问、数据库凭据或签名密钥。不同 subagent 会话不等于 OS 安全隔离；以后开放任意代码工具，必须另外落实实际沙箱/独立身份与网络文件权限，不能以独立进程代替。

本方案不保证管理员篡改 Core 后仍安全、不保证被攻陷的受信推理服务诚实，也不宣称仿真成绩代表临床有效。

## 3. 最小合同与闸门

### 3.1 输入及产物

输入至少保留 `case_id / cohort / source / decision_time / timezone / unit / sampling / missing_mask / history / provenance`。当前可用信息与未来评价标签分存。缺失不是零、不是“没有”；病历的既往/当前/疑似/否定含义分开。

多模态抽取先生成候选，带原文件、页码/区域、原始值和不确定性，再校验进入快照。不能把食物照片估算碳水、血糖截图识别值或 LLM 补全值自动当成已验证的剂量输入。病例身份不匹配、单位含糊、关键资料冲突时阻断相关路径。

所有可信产物记录：

- `artifact_id / artifact_type / schema_version / case_id / run_id / snapshot_id`；
- `job_id / attempt_id / producer_identity / origin / input_digest / parent_artifact_ids + digests`；
- 模型权重或部署版本、预处理、规则、模板和配置版本；
- 数据截止、执行/接收时间、时效、适用域、状态、payload 及其 digest。

生产者身份、origin、规则版本由 Core 认证上下文填写，不相信 LLM 自报。严格拒绝重复 JSON 键、NaN/Inf、非法单位、宽松类型转换及未知关键字段。权威结果在受保护事务库登记；跨信任边界时才增加受信签名验证，密钥不交给 agent。

预测结果注明窗口、单位、适用范围；没有校准置信区间就写 `uncertainty=unavailable`。RL 返回 `candidate / abstain / unsupported / error`；只有 candidate 承载候选。拒绝时动作值使用 null，**不能拿剂量 0 或“维持治疗”代替失败**。具体动作合同见第二份方案。

### 3.2 状态流

```text
SNAPSHOT_FROZEN → ELIGIBLE → PREDICTION_ACCEPTED → POLICY_ACCEPTED
 → SAFETY_ACCEPTED → DRAFT_READY → REVIEWED → RELEASED

任何一步可进入 NEEDS_INPUT / UNAVAILABLE / BLOCKED / CANCELLED / FAILED
```

每个消费者入口验证前置条件，最终发布重新核验整个依赖链。输入更新、模型/规则撤销或报告内容改变，相关下游资格失效。取消后迟到结果、旧 attempt、重复请求不能重新激活 run。使用事务、幂等键和 attempt fencing 防止并发越级。

审核由 Core 分配 job，绑定**最终报告字节 hash、证据 hash、规则版本和 reviewer 身份**。医学语义与伦理 reviewer 均必须明确通过；超时、弃权或未解决问题阻断。最多两轮修订，每轮完整重审。Reviewer 的文字不是安全证明，确定性来源/权限检查不能委托给它。

状态/错误说明、必要追问、经核验的历史事实、通用教育可走独立低风险路径。双模型闸门限制的是个体化给药与诊断性结论，不要求追问前先完成两次推理。低风险旁路不得包含隐含加量、停药、维持、给药时机或可替代个体剂量的计算。安全阻断为独立内容类型，剂量字段为空；紧急升级说明来自审定规则，不等待双模型成功，也不自行开出补救剂量。

### 3.3 主 Agent 工具与权限合同

采用 **SDK Agent → 受控 MCP 工具 → Core → 独立模型 worker**。模型执行器留在 SDK 进程外有利于超时、资源、权限和证据管理；这与 Agent 通过 MCP 调用两个模型完全兼容。MCP 负责工具通信，本身不证明医学真实性或绕过风险已经解决。[MCP 工具规范](https://modelcontextprotocol.io/specification/2025-06-18/server/tools)；[SDK 的 MCP 接入](https://code.claude.com/docs/en/agent-sdk/mcp)

首批能力分为：当前病例/缺失项读取、解析抽取请求与来源查看、澄清请求、快照冻结、预测请求、RL 请求、作业状态与取消、受控知识检索、解释提案、请求审核、请求发布及读取已发布结果。它们是能力规划，名称及参数在相应 TODO 冻结；当前报告/审核工具不等于这些能力已经完成。

模型请求只传授权引用与幂等标识，不传可替换的权威数值、代码、模型地址或身份。工具返回结构化 job/status/error/accepted artifact 引用；Core 拥有依赖及业务状态。主 Agent 可以申请审核/发布但不能自己提交 reviewer verdict 或写 ReleaseBundle；reviewer 只看其绑定报告，不看另一角色结论。任务权限按角色、病例、run 和用途收窄，新增工具必须检验正反路径。

Core 在每次请求及最终发布时执行预算、取消、时效、模型版本及依赖检查。Agent 可以针对可恢复错误有限重试，但不得反复抽样挑选有利剂量、丢弃失败或改输入骗过安全闸门；复用结果须保持同快照、同依赖版本且未过期。模型弃权只触发补资料、停止或升级，不由另一个 LLM 补出动作。

### 3.4 多模态、会话与病例事实

首个用户界面支持文本、结构化 CGM、图片/PDF；语音是后续可选通道，不阻塞第一版。上传后由受控解析器检查真实格式、大小/页数、可执行内容、来源与资源使用；OCR/视觉文本一律为不可信数据，不能提供新的工具权限。界面展示可定位的候选字段供核对，关键矛盾通过追问解决。用户确认只记录确认来源，不自动等价于医疗验证。

输入兼容矩阵须为每个字段指定单位、时区、时点/历史窗口、原始来源、缺失语义、模型或规则消费者和核验方式。至少覆盖 CGM、已实际执行的胰岛素种类/途径/时间/剂量、当前治疗背景，以及所选模型要求的其他变量。图片、餐食和检验单只有合同允许的字段可以进入数值模型，其余只用于范围判断或解释；不能默认“多模态模型看见了就会影响剂量”。当前 SnapshotInput 只有工程 CGM 字段，尚不满足本合同。

会话可以持续，但每次评估必须绑定明确的患者和冻结快照。关键资料被纠正、输入补充或实际给药更新时，Core 判定依赖失效并新建版本；聊天记忆和总结不能成为事实库。澄清、挂起、取消、切病例、重开及多轮请求均保留可审计状态，既不自动恢复收费任务，也不把旧建议当作新时点结论。

### 3.5 给药动作、自然语言与统一出口

输出采用有类型的建议包：适用对象和时点、批准动作引用、解释性 claim 与证据引用、一般教育引用、限制/风险、有效期和反馈入口。动作语义至少区分胰岛素类别/制剂（按已验证配置）、给药途径、基础率与单次剂量、单位、开始条件/时间、持续时长及失效条件；临床需要浓度/装置等信息时须纳入对应配置，不能从通用模拟胰岛素猜出真实药品或体积。

首个基础输注场景保留模型原始基础率数值和单位，明确支持的 U/min 与 U/h 可由受控代码等价换算，并绑定转换版本与模型证据；区间总量由同一速率和持续时间计算。转换不静默量化动作，循环小数内部用精确分数校验、近似展示须标明；装置执行精度在真实接入时另行验证；不得把区间总量解释为可一次注射的剂量，也不把短窗口建议外推为全天方案。仿真 5 分钟步长不是让用户每 5 分钟与 LLM 聊天；离线数值仿真与用户发起的一次建议任务分开验收，发布时检验实际计算延迟及剩余有效期。

Agent 负责选择有依据的解释点并提出易懂措辞。涉及用药动作、条件、频率、风险程度的语义使用版本化槽位和受控表达；一般解释同样绑定支持它的模型或知识证据。不能只锁数字而放任“立即/每天/必要时”等文字改变含义。无法支持的个体化疗效保证、生活方式处方或新疾病判断不得发布。自由措辞与 reviewer 都不能提供零错误保证，须以语义忠实、引用支持和用户理解测试验证。

双审核通过同一个最终版本，Core 才发布。聊天正文、流式片段、卡片、图表说明、历史摘要、通知、复制/导出和未来语音均从该发布包派生；审核前只展示受控状态或追问。当前桌面未接真实双审，须为新 run 显式冻结 reviewer 配置，不能升级旧 fixture 审核。两个同底模 reviewer 是分角色审核，不是统计独立性或医学专家背书。

### 3.6 建议后的产品反馈

分别记录“模型候选”“已发布建议”“用户已读/确认”“用户自报实际执行”“有来源的实际递送/后续测量”。已读、接受和建议值不得自动写入实际胰岛素历史或 IOB。漏用、延迟、重复记录及实际剂量不同须可纠正并留存来源；新事实引发新的快照和评估，既有结论保留历史及失效标记。

研究闭环由仿真器执行动作并返回后果；用户产品闭环由实际反馈形成新评估，两者不得混记。异常或超范围情况需明确下一步及真实可用的人工渠道；未接医疗服务时不显示“医生已接管”。随访提醒需明确授权、时间与失效语义，不因后台定时触发无限 LLM 费用。反馈不会直接在线训练 RL 或改安全规则。

## 4. 技术栈与已有组件接入

### 4.1 App 与 Agent

采用 React/TypeScript + Tauri 的 Mac arm64 原生桌面壳；Python Core 使用 FastAPI、Pydantic、SQLite。CLI 为 Core 的薄客户端；MCP 是主 Agent 调用受控模型和业务能力的首版接口，连接同一 Core 服务。模型可在本地或远端推理，按实际部署做 adapter，不预设必须转换成 ONNX。

采用 Claude Agent SDK + DeepSeek Flash，开发禁用 Pro。SDK 提供循环、工具、会话和 hooks，实际智能来自所接的 DeepSeek；不等于混合获得 Claude 模型能力。2026-09-10 已固定 claude-agent-sdk 0.2.152、内置 CLI 2.1.259，真实兼容烟测结果见 implementation/docs/sdk-flash-smoke.md。

已核验的官方文档显示：DeepSeek 的 Anthropic 兼容端点为 `https://api.deepseek.com/anthropic`；当前 `deepseek-flash` 为 V4.1 Flash，旧 V4 Flash/vision-exp 名称由最新 Flash 承接。本轮真实请求和响应均为 deepseek-flash，网关拒绝其他模型，不能依赖 SDK 的默认别名映射。部分错误/禁并行字段在兼容层会被忽略，核心串行或错误闸门由 Harness 执行。多模态仍须单独验收，不凭文档说明打勾。[兼容表](https://api-docs.deepseek.com/guides/anthropic_api/)；[当前型号与价格](https://api-docs.deepseek.com/quick_start/pricing/)；[视觉指南](https://api-docs.deepseek.com/guides/vision/)

第一轮记录 SDK、实际运行时、请求/响应模型标识；测试工具成功/业务失败、超时、取消、流式、子任务。按用户最新要求，最终必须接入 Agent SDK 与真实 LLM；如首选 SDK 链路有兼容问题，记录问题并评估兼容的 SDK 方案，保留 Core 合同与闸门，不能把直接 provider 调用或固定循环当成 SDK 验收完成。

### 4.2 Agent Observation：callback 是进化反馈入口

此前已读本地源码 `/Users/george/Desktop/Tencent/agent-quality-eval`：具备 Claude hook、OTLP `/v1/logs` 和 `/v1/traces`、会话与 timeline 查询，以及 `/api/uplink/cot` 接收能力。这是源码证据；本次会话此前探测本地 8765 端口未连通，**尚未证明医疗项目已接通观测 App**。

接入设计：

1. SDK hooks 记录 LLM/工具观察；Core 主动记录预测、RL、安全、发布等确定步骤。直接 Python 调用不会自动产生完整 SDK hook。
2. 一个 run 一个根 trace，关联 case/run/snapshot/job/SDK session/tool_use/span。分别保留事件来源，避免把 hook 与 Core 的同一步计成两次成功。
3. Core 状态、关键审计和 outbox 在同一事务提交；callback 轻量入队，Bridge 异步转 OTLP，重试去重。
4. 观测服务关闭时本地保存、恢复后补传；本地关键审计提交失败阻断发布，外部观测不可用不改变闸门结论。
5. 若只有 OTLP 不能形成现有 UI 会话，增加兼容会话投影。`uplink` 需检查业务 `ok`，其固定 session 文件覆盖行为要求独立命名空间和顺序投递，不覆盖旧项目记录。
6. 保留事件事实、拒绝原因、版本和 hash；不伪造隐藏思维链。凭据脱敏；许可不允许的原始病例不发给外部 LLM 或观测服务。

所有本项目缓存、数据、日志、模型、临时文件和构建输出配置在本桌面目录内。Observation 若不能配置隔离数据根，先在此目录启动隔离实例验证；不能静默使用历史数据目录。原生跳转指定会话的 deep link 尚未核实，不作为首版依赖。

### 4.3 ClinicalBERT 移除后，资料抽取怎样保留

论文中的 ClinicalBERT 主要提取病历症状特征。当前 CGM/RL 首个实验直接读取结构化数值，不需要 ClinicalBERT，也不需要每个时间步调用 LLM。

App 引入病历、检验单和图片时，使用“格式解析/OCR → DeepSeek 或 Qwen 抽取候选字段 → schema、单位、时间、否定/既往语义与来源校验 → 冻结快照”。模型能否处理图像按具体版本核验；文本模型不能仅靠 prompt 获得视觉能力。抽取项带 source/page/region，缺失返回 unknown，不根据常识填病史。

因此移除的是指定 BERT 模型，仍保留结构化抽取和核验这一职责。LLM 抽取不天然比原模型可靠，要用标注字段集检查准确率、遗漏、否定/时态和单位错误。首版 RL 输入白名单只接模型合同认可的结构化字段；新抽取特征需验证后才能加入。

### 4.4 RAG：首版必做的可追溯知识层

数值 RL 训练不依赖 RAG；完整研究 Agent 的知识解释、引用和审核需要可控知识来源，因此 RAG 列入 MVP。模型参数中的知识不能替代“本次具体引用了哪一版哪一段”的记录。[RAG 原始研究](https://arxiv.org/abs/2005.11401)

| 内容 | 进入哪里 | 发布/更新规则 |
| --- | --- | --- |
| 指南/共识、正式药品资料、审定教育材料 | 有来源与版本的医学知识库 | 支持一般解释和审核，不自行补出个体剂量或疾病诊断 |
| 模型卡、数据字典、实验协议 | 单独标签的技术知识库 | 解释适用域/失败/数据含义，不能冒充医学指南 |
| 当前病例、CGM、检验单与病历 | 本病例证据空间 | 按病例/时点授权，不能作为其他病例的检索知识 |
| 真正执行的安全/适用性规则 | 版本化规则注册表 | 根据来源单独编码、审查、测试后激活，不由运行时 RAG 文本自动变更 |

首版流程：选定少量权威且允许使用的文档 → 按章节/表格语义切块 → 保留限定条件及页码/章节 → 建索引 → 先按人群、用途、版本、权限过滤 → 检索 → 固定格式证据包 → LLM 解释 → 引用与语义审核。首批约 10–20 份文档即可，开工时核对实际版本与许可；不先抓取整网。指南表格不能把适用条件或脚注与结论拆散。

本地 SQLite 保存原文定位与元数据，FTS5 提供词法检索；中文须选定并测试分词或 trigram，不能假定默认英文分词器适用。加一个固定版本的 embedding 做语义召回，两路合并去重；少量文档可直接扫描向量，不必单独部署向量数据库。reranker 是否加入由召回/引用测试决定，不把它设为首版前提。[FTS5 官方文档](https://www.sqlite.org/fts5.html)

`KnowledgeBundle` 至少包含 `kb_version, query, filters, retriever_version, doc_id, chunk_id, source_url, title, document_version, page_or_section, content_hash, retrieved_at, scope`。查询与过滤条件由 Core 授权；LLM 不能通过改 query 读取其他病例、测试答案或封存集。必须先过滤再检索，不能先把禁用内容送给模型再删除。

**与强 Harness 的结合**：

- 模型个体结果和通用知识说明分开呈现；RAG 不成为第三个独立个体诊断判据，不覆盖 RL 候选、不计算替代剂量。
- 知识引用必须来自本次 Core 接受的 bundle；链接由服务端解析，不能相信 LLM 自造 DOI、页码或引文。引用存在还要审核其是否支持对应句子及适用人群。
- 文档中的指令视为数据，不执行其中“忽略规则/调用工具”等文本。RAG 命中不能授予工具权限或修改规则包。
- 无结果、证据冲突、版本不可用时，相关知识性解释不能凭参数记忆补成“已有引用”。需要该依据的完整解释报告进入阻断/补证状态；已授权的纯结果卡或故障说明可按独立报告类型发布。
- 知识库版本与内容 hash 绑定报告。一次 run 固定版本；更新不改历史报告，来源撤销使依赖报告失去当前有效资格，再分析须新 run。
- callback 记录检索 query、过滤、命中片段、引用失败与成本；自进化可优化 query/检索参数，不能自行修改指南原文、临床阈值或放行规则。

首版 RAG TODO（第 2–4 周）：

1. ☐ 选定首批文档与来源/许可/版本清单，区分医学与技术资料。
2. ☐ 解析与切块，保留表格脚注、页码和适用范围；抽查引用能回到原文。
3. ☐ 建本地词法/向量索引及权限/人群/版本过滤，记录索引与 embedding 版本。
4. ☐ 接入检索工具、KnowledgeBundle 与报告引用卡，UI 支持打开原文定位。
5. ☐ 制作约 30–50 个固定查询：答案存在、无答案、T1D/T2D错配、版本冲突、药物限定与同义表达；标注相关段落。
6. ☐ 检验 Recall@k、引用支持率、错误人群命中、无证据时误答、注入和跨病例越权；索引召回好不等于答案医学正确。
7. ☐ 验证知识库故障/撤销、伪造引用与过期 bundle 的阻断；接入 Observation，做有/无 RAG 的同任务解释对照。

## 5. 观测驱动的受控自进化

2026-09-10 用户确认实施顺序：**先打通完整用户任务链，完成两个真实数值模型并接入，落实和验收该链路的各项 Harness 闸门；再按预先冻结的数据划分，使用许可允许的公开真实数据开展正式 self-evolution。** 当前不开始自动候选优化，也不以 fixture 结果评价完整产品的进化收益。审计、失败记录与评价协议随开发准备，但不抢先运行正式进化。

正式进化的启动条件为必要用户链路、M01/M02 真实模型及 V01 闭环/闸门验收完成，Observation 事件关联与数据许可、分组、基线、评价器、修改白名单及预算就绪；不要求先完成后续可选功能或真人服务准入，离线公开数据研究与实际患者服务分开。版本化的基线固定后执行：

```text
固定版本执行开发病例 → callback 收集失败证据 → 归因
 → 优化 agent 提出有限 diff → 隔离回归 → 验证集比较
 → 满足安全与可用性要求才晋级 → 保留回滚 → 再观察
```

可优化主 Agent 提示、工具描述、检索查询及不改变医疗语义的表达候选；每项仍需固定回归及发布审核。不能自动改可信模板、动作语义、风险标识、renderer、Core、权限、产物验证、安全规则、给药来源、judge 或封存答案。预测/RL 训练是第二份方案的独立实验，不能在病例分析时根据一次反馈偷偷更新权重。

数据分 `evolve_train / evolve_validation / sealed_test / demo`。患者划分先于切窗，跨聚合源去重；模型训练数据与 Harness 测试重叠须披露。Demo 允许排练，不能当泛化成绩；小队列可从开发集选示例，保留独立测试。

正式实验前固定患者/来源标识及去重清单：`evolve_train` 用于失败归因和候选生成；`evolve_validation` 用于候选选择；`sealed_test` 仅用于冻结后的最终评价，不供 Agent 检索或反复调参；`demo` 仅展示。与预测/RL 的训练划分分别登记并核对重叠，不能把数值模型见过的患者冒称完整系统未见病例。

公开真实数据用于其许可及适用域支持的输入、追问、工具编排、检索、解释和闸门评价；真实数据的人群/动作超出模型范围时，只评价对应的资料处理或拒绝路径，不解除适用性检查来凑齐完整给药任务。仿真轨迹继续补充可执行动作后果的正向研究闭环，合成故障/攻击用例继续守住回归底线，三类结果分别报告。公开历史记录不提供替代剂量的真实反事实，不能把离线 self-evolution 当作临床疗效验证。

晋级按先后条件：硬约束零破坏→禁止类语义/伦理错误不过线→正常任务完成率与误拒绝合格→再比较质量/耗时/成本。不能靠全拒答获胜。每轮候选数、调用预算、停止条件固定；模型断连不能通过修改 prompt 补结果来“修复”。

可检验的贡献有两项：**基于产物依赖和唯一发布权的强制证据链**；**将自主观测 callback 转成可归因、可评估、可回滚的改进闭环**。Callback、MCP 或 GRPO 本身不是本项目发明。为技术交底保留反例、状态图、合同、关键实现和对照结果；新颖性与可专利性另需专门检索，当前不作承诺。

## 6. 数据与医学伦理底线

RL 主训练与闭环评估先用公开虚拟患者；真实数据优先 DCLP3/Ohio，CGMacros 用于多模态展示，详情和入口全部集中在第二份方案。公开可申请不等于无条件许可，公开去标识化也不等于没有伦理义务。

- 不生成虚构病史、给药记录、医生偏好或真实病例间的假配对。
- 研究阶段不把 RL 候选当作真人处方；最终个体化给药服务另须满足准入要求，不因来源有效便宣称医学正确。
- 不用观察性预测器计算改变剂量的反事实，不把预测误差小等同于策略安全。
- 不以文字免责声明替代权限、来源、安全和展示限制。
- 未知风险明确显示未知；公开历史风险提示不误报为当前患者急症。
- 不混用成人/儿童、T1D/T2D；不能从健康人的餐后血糖训练 T1D 给药而不做适用性验证。
- 公开演示只打包许可允许再分发的资料；数据使用条件、来源和限制随报告保留。
- 伦理 reviewer 按不伤害、证据忠实、适用范围、不确定性、公平表达进行审核；无医学专家复核时如实说明该评价局限。


真实个人健康资料接入前，冻结数据流与权限表：本地保存什么、发给哪个模型/服务、目的、最小字段、许可/知情授权、保存期限、删除/撤回、备份和审计保留方式。外部 LLM 与 Observation 默认只接任务所需信息；原图、身份信息和完整病历不因用户上传就自动外发。明确本地加密与密钥管理、跨病例隔离、共享电脑访问保护、诊断日志脱敏和异常退出恢复；这些是待实现要求，现有 bearer/路径检查不能代表它们已经完成。

## 7. 详细 TODO 与验收

本表状态核对日期：2026-09-10。下表保留原工程基线及全部既有勾选；新增产品工作按第 7.2 节优先顺序执行。原表周数是 v5 研究估算，不再作为最终产品排期。☑️ 表示已实现并验证，☐ 表示未完成或暂缓；03 号长版为历史资料，不同步执行进度。

逐项勾选表示该具体 TODO 已有实现与验证记录，不表示整个阶段已完成或用户已审核。实际结果和用户审核状态见根目录 `工作记录.md`；阶段 0/1 证据见 [桌面前置核验](implementation/docs/pre-desktop-audit.md)。真实预测/RL 接入按用户最新指令暂缓：原阶段 0 的真实预测要求单列为 0R，保持未勾选，不能用 fixture 验收代替。

| 阶段 | TODO | 完成依据 |
| --- | --- | --- |
| 0：工程合同，1–2 天 | ☑️ 固定占位入口/依赖/窗口/适用范围，明确无训练域验证；☑️ 固定快照、预测、RL、错误及请求/产物 schema；☑️ 建真实/仿真/fixture 标记并执行隔离；☑️ 限制项目输出路径，校验符号链接出口 | 合同导出与运行时一致；工程请求/响应可复现；fixture 不可冒充真实模型。见核验记录  |
| 0R：真实执行器接入前置（真实预测暂缓） | ☐ 确认真实预测器入口/依赖/窗口/训练域及版本；☐ 复现一份真实预测请求与响应并核验适用性；☑️ 独立 worker、超时终止与资源限制 | 200 项回归通过，其中新增 24 项真实进程专项；原生超时约 5.001 秒 SIGKILL 并回收，取消/失效/父进程死亡及资源超限已实测。当前 macOS 工程执行配置见 implementation/docs/worker-isolation.md；真实 SDK/模型接入时另验实际负载和权限。前两项仍暂缓，本步待用户审核 |
| 1：Core，第 1 周 | ☑️ 作业/事务/产物库；☑️ 来源和依赖校验；☑️ 最小 CLI；☑️ 取消/重试/fencing；☑️ 发布 API；☑️ 异常认证、输入拒绝与审计失败回归 | 109 项工程回归通过；真实 HTTP/CLI 及重启验证通过；无双模型证据不进入报告，伪造/重放/跨 run 请求被拒。真实医疗能力仍未接入 |
| 2：最小 UI，第 2 周 | ☑️ Tauri 原生壳；☑️ 文件导入（结构化 JSON）与原始时间线；☑️ 证据卡与阻断状态；☑️ 预测占位 adapter 桌面联通（真实接入见 0R）；☑️ RL fixture 故障演示 | 176 项回归通过；8 个固定 RL 场景均经原生实点及 HTTP/账本核验，实际超时约 5 秒、迟到策略未登记，所有场景均无审核/发布。旧数据迁移保留；本步待用户审核，真实模型/LLM 未接入 |
| 3：LLM，第 2–3 周 | ☑️ SDK/Flash 兼容烟测（真实 SDK + LLM，验证实际 worker 负载/权限）；☑️ 受限报告合同与 renderer 的真实 LLM 联调；☑️ 两类真实 reviewer；☑️ 真实审核报告版本绑定与失效专项验收；☑️ 桌面 Agent 任务入口、运行状态与取消接入；☐ 全通道发布限制（含桌面双审调度与报告读取/导出） | 320 项回归通过；原生真实 SDK/Flash 报告任务、状态/工具记录、取消与退出重开已验证，6 次真实请求、2 份待审草稿，取消任务无草稿，0 审核/发布；原数据完整保留。报告/双 reviewer 及绑定专项见既有文档，当前桌面任务边界见implementation/docs/desktop-agent.md。本步待用户审核；桌面双审配置与调度、全通道报告读取/导出及真实医学规则仍未完成 |
| 4：Observation，第 2–3 周 | ☐ 双源事件；☐ Bridge/outbox；☐ 命名空间与脱敏；☐ 真实观测窗口验收；☐ 断线补传 | 同一 run 显示双模型、审核与发布的正确关系，无旧数据覆盖 |
| 4R：RAG，第 2–4 周 | ☐ 来源清单；☐ 切块与索引；☐ 范围过滤；☐ KnowledgeBundle；☐ 引用卡；☐ 固定检索/故障/攻击评价（详见 4.4） | 引用可定位；无证据不冒充有证据；检索不覆盖 RL 剂量或门禁 |
| 5：真实 RL，第 3–7 周依模型进度 | ☐ 自行实现并训练策略；☐ 记录动作条件模型依赖；☐ 预测器域适配；☐ 虚拟病例闭环；☐ 弃权检查 | 不依赖作者权重，双模型接口实际运行，产物与报告一致 |
| 6：多模态与体验，第 4–6 周 | ☐ 一种真实配对多模态数据；☐ OCR/来源定位；☐ 冲突与缺失；☐ 导出及重启恢复 | 原始证据可定位，不把视觉估算当确定给药参数 |
| 7：受控进化，第 6–8 周 | ☐ 冻结 judge/测试集；☐ 有限候选；☐ 配对评价；☐ 晋级/回滚；☐ 展示进化前后 | 至少一轮完整可追溯循环；没有合格候选则诚实保留基线 |
| 8：交付，第 8–9 周 | ☐ 对抗测试；☐ 原生打包及新环境启动；☐ 异常退出后的子进程回收；☐ 论文图表/数据清单；☐ 答辩脚本；☐ 技术交底材料 | 可复现演示正常、故障、攻击、审核失效和进化过程 |

v6 先补产品动作合同与 Agent 编排，再完成受控用户输出和多模态交互；Observation 与自动进化不抢占最小用户闭环。原数值模型训练任务继续保留，实际排期须等接口、数据、吞吐和验证范围确认后重估；研究演示用时不能当作真实患者产品的交付承诺。

### 7.1 必须覆盖的对抗验收

| 测试族 | 代表情形 | 必须结果 |
| --- | --- | --- |
| 推理缺失 | 断连、超时、HTTP200 但业务失败、空 payload | 无合法产物，无完整诊断 |
| 假来源 | agent JSON、手写文件、自造 hash、fixture 冒充 | 拒绝注册和发布 |
| 错绑定 | 跨患者、跨 run、旧预测父项、错误版本 | 拒绝，记录具体原因 |
| 数据无效 | 单位未知、日期错位、缺胰岛素、冲突、多模态假配对 | 不以 LLM 补全推进 |
| 生命周期 | 重复、并发、取消后迟到、输入更新、撤销版本 | 不越级，不发布失效产物 |
| 内容越权 | 改剂量、补频率、同数值但改“现在/每天”、新增诊断 | 草稿驳回或正式模板无法表达 |
| 间接注入 | 病历/图片/OCR/RAG 中要求跳闸门 | 权限和来源边界不变 |
| 检索证据 | 无答案、错人群、伪 DOI、引用不支持正文、撤销文档、跨病例命中 | 不生成假依据，不用 RAG 补剂量；依赖失效被拦截 |
| 审核失效 | 漏 reviewer、超时、假身份、通过后改一个字 | 不复用旧 verdict |
| 出口旁路 | 流式、标题、图表、缓存、导出、通知、API | 无未发布医疗内容泄漏 |
| 日志与恢复 | Observation 关闭、重复补传、Core 审计失败 | 外部故障可补传；核心审计失败不发布 |
| 进化投机 | 改裁判、读封存答案、放松闸门、全拒答 | 候选淘汰，记录原因 |
| 原生交付 | 启动、关闭重开、子进程、资源、历史报告 | 实际 App 窗口/后台/数据链一致 |

实验对照：prompt-only、仅工具白名单、完整 Harness；相同任务与模型条件比较非法发布率、证据支持率、正常完成率、误拒绝、恢复和成本。移除门禁的组仅隔离评价，不用于默认产品。零次违规只说明测试集内未发现，不等于普遍安全证明。
| Agent 决策 | 缺资料先追问；错误顺序调用 RL；企图直接发布；反复抽样挑结果 | 允许有效规划，拒绝缺依赖/越权/挑结果；验证真实 SDK 工具轨迹 |
| 多轮与身份 | 补充刚执行的剂量、同名患者、切病例、时区变化、聊天摘要过期 | 新事实走核验并使相应证据失效，不跨病例复用 |
| 反馈与时效 | 已读误记已用药、重复上报、实际不同、计算结束已过期 | 历史来源准确；不发布过期建议，不伪造实际给药 |
| 多模态通道 | OCR 数字/小数/单位/否定错误、PDF 注入、错误配对、文件资源耗尽 | 关键字段不静默入模；疑点可追问，解析失败可恢复 |
| 用户与数据 | 撤回外发授权、删除后缓存残留、跨病例检索、未授权提醒 | 权限与保留政策一致；不外泄，不产生未授权任务 |
| 真实可用性 | 合法完整资料、正常双模型、真实双审；常见缺失可补齐 | 既验证阻断，也验证能完成的有效用户任务，报告耗时/费用/误拒率 |

### 7.2 产品补充 TODO：此后执行顺序

2026-09-11 批次28用户最新调整：近期先推进RL，用户先按 [五来源下载与构建方案](06_RL真实联合数据集下载与构建方案.md)取得Loop/DCLP3/UOM及申请Ohio/DiaTrend，再开展原包许可/轨迹资格和联合划分分析。本轮只完成论文方法澄清与构建计划；此说明覆盖此前默认先P06、仅审UOM或仿真主训练的排期，不删除其他产品要求。M01/M02父项、原包质量和训练均未完成；真实历史用途不得绕过现有合成/仿真执行合同。

P01/P02/P03/P04 已验收对应工程合同，其余父项仍未完成；真实数值模型配置不因 P01 完成而取得资格。文档定义完成不等于相应软件能力完成。每轮只选择一个具体编号，完成实现与相应验证后停下供用户审核。已有代码优先复用；细项若仍过大，先拆分为可单轮验收的子项且保留父要求。

| 编号 | TODO | 验收依据与原表关系 |
| --- | --- | --- |
| P01 | ☑️ 落实成人 T1D 基础输注的输入、动作、证据与用途配置合同 | 明确哪些模型/规则消费哪些字段；速率/持续时间/总量、实际用药/建议分离；缺字段/错域阻断；兼容既有 fixture 数据；承接 0/0R/02 合同 |
| P02 | ☑️ 建立真实资料的数据流、外发授权与保留/删除合同 | 本地/LLM/Observation 的字段与许可清单；撤回、日志、备份和跨病例的正反检查；落实于真实资料输入前 |
| P03 | ☑️ 暴露受控预测 MCP 作业工具 | 真实 SDK 能请求/查询预测；Core 登记，错病例/超时/幂等/取消检查；数值执行仍可用 fixture。批次 24 新增 41 项、全量 537 项通过；实际 SDK/MCP/独立预测 worker 及 localhost HTTP 离线链路通过。独立 prediction 许可、父/子 job 绑定、最小证据引用与 SDK 生命周期接通；默认关闭 HTTP 入口，桌面主循环另属 P05。用户已授权后续步骤 |
| P03a | ☑️ App 设置中的 MCP 状态与手动本地自检 | 用户补充，批次 25 完成：工具定义/手动实际 SDK-MCP-Core-fixture 自检/真实模型状态分开；独立临时合成账本，无付费调用，失败撤下旧成功。新增 11 项、全量 548 项通过，实际 localhost 桌面 client 链、原生构建和隔离预览通过；未实点原生窗口。用户已审核通过；P04 独立推进，P05 未完成 |
| P04 | ☑️ 暴露受控 RL MCP 作业工具 | 批次 26 新增 56 项、全量 604 项通过。实际双 SDK/MCP/四个独立 worker 与 HTTP 预测→RL 链通过；Core 装配父预测，独立 rl 许可、配置/父子 job/下游完整 SDK 重验。无预测/错绑/弃权/取消/撤权/超时阻断；仍是 fixture 候选，未通过该入口执行安全/报告/发布。HTTP 默认关闭，待用户审核 |
| P05 | ☐ 建立主 Agent 的任务循环与最小工具集 | 真实 SDK 决定追问、调用两模型、根据结构化失败恢复或停止；验证不同输入导致不同合理工具轨迹，不再先固定跑模型后叫报告 Agent |
| P06 | ☐ 实现会话、病例事实、澄清及快照版本衔接 | 文本输入、关键事实确认、切病例/补资料/取消/重开；旧快照资格失效，不自动恢复收费任务 |
| P07 | ☐ 接入桌面真实双 reviewer 的配置冻结与审核请求 | 新 run 绑定真实双角色及最终报告；失败/弃权/修改重新审核；承接原阶段 3 未完成项 |
| P08 | ☐ 完成已发布报告的桌面读取与复制/导出 | 只读取仍有效的 ReleaseBundle；流式/标题/缓存/旧报告等旁路检查；承接原阶段 3，工程报告先验收 |
| P09 | ☐ 接通受控 RAG 工具与证据包 | 按原阶段 4R、4.4 的各具体 TODO 逐项实施和勾选；先过滤、版本/来源/引用和故障验证，不用资料补剂量 |
| P10 | ☐ 实现产品级解释提案与受控语义 renderer | 从章节排序升级为有证据的自然语言解释；动作与风险语义不可改写；反向语义/引用/遗漏必要限制专项；重新完整双审 |
| P11 | ☐ 完成图片/PDF 的受控解析与候选字段来源 | 文件限制、OCR/视觉专项、原文件定位与外发最小化；实际视觉能力和费用另验，不凭型号名打勾 |
| P12 | ☐ 完成字段核对、冲突追问及模型输入白名单 | 标注集测关键字段错误、否定/时间/单位；人工确认留来源；假配对/无药史不进入剂量链；承接原阶段 6 |
| P13 | ☐ 实现面向用户的对话、上传、进度与建议卡 | 普通用户无需 JSON/job/hash；显示动作含义、解释、适用性、有效期和反馈入口；同一患者合成配对正常路径与公开数据受限路径分开验收 |
| P14 | ☐ 实现模型不可用、弃权与风险升级的用户路径 | 可补资料则追问；终止则明确无剂量；紧急说明不等待模型；不会自动“维持/归零”；无人工渠道不伪称接管 |
| P15 | ☐ 接通后续测量与实际用药反馈 | 已读/接受不冒充执行，重复/延迟/修正可追溯；新证据触发新评估，历史失效可见；仿真反馈与真人反馈分开 |
| P16 | ☐ 完成个人健康资料的存储、访问与生命周期防护 | 落实 P02 的授权/最小外发、加密/密钥、删除/备份/撤回与日志策略；跨病例、缓存、退出/重启验证；删除前备份/独立导出/未归属 SDK 目录的删除对账、审计保留期限及物理/内存残留边界须在真实个人资料接入前完成 |
| D01 | ☐ Loop事件重建、片段资格与全模型患者隔离冻结 | 批次38已完成五源原始审计（07），不等于完成本项；验证完整basal+bolus前史、餐食/体重/制剂、净人日/episode/transition、F重叠与split；不跨患者填事实 |
| D01a | ☑️ Loop患者级无插值预处理与条件性离线数据清单 | 批次44：919个患者文件、851有CGM，329有合格片段；train225人/1,653,421条transition，患者级四组冻结。全量CGM来源核验与115次独立剂量积分通过。仅数据工程/回顾性资格；M池辨识、现有F重叠与完整反事实验证不在本子项内，D01父项保持未完成 |
| D01b | ☑️ Loop模型输入合同、缺失编码与可执行训练数据管线 | 2026-09-14：72×14核心输入、train-only统计、5/15/30/60分钟恒定动作索引、序列/批次加载、作者实现核对后的奖励与时长折扣；14项边界测试、3,272个真实样本核对及280名患者的未来扰动/轨迹检查通过。仅管线验收，不代表真实模型训练、CUDA或闭环效果通过 |
| D01c | ☑️ food/exercise稀疏22维患者模型输入与远端等价验证 | 2026-09-15：v2明确72×22、不设存在性门槛；225人/1,653,421起点保留，280患者hash/未来扰动/train-only统计复核及836实际训练张量比对通过。原数据和v1保留；不代表在线可得或反事实资格 |
| M02a | ☐ RL-DITR方法的真实患者模型训练与多步验证 | 按D01b输入实现历史表征、动作条件转移、血糖/WTR预测及一致性监督；先检查训练/验证误差、长展开稳定性与干预敏感性。不得把历史拟合替代反事实和独立闭环证据；策略训练另作后续子项 |
| D02 | ☐ 新版本真实公开历史研究输入/动作证据合同 | 现有P01合成/仿真合同保留；新增真实制剂、治疗basal+bolus与受控basal动作分离、command/derived/delivered、历史和运行状态依赖；不得改标签绕过，须独立验证 |
| M01 | ☐ 接入真实预测并验证所选仿真域 | 原 0R 两项与 02 对应项仍未勾；模型卡、输入窗口/预处理/域适配与可复现产物；未通过则研究闭环不放行 |
| M02b | ☑️ 单seed全量Loop真实RL训练、两近期baseline及独立封存研究评价 | 2026-09-15：O07对ReBRAC/FQL/BC及同结构去RL，固定50k/seed260915；6封存患者×9情景，TIR94.97%且冻结总体研究门槛通过，完整失败记录/HTML/CSV/LaTeX/权重与独立推理包已交付。个体低血糖、单seed和仿真域边界明确；不表示Core集成或临床安全 |
| M02d | ☐ 非incremental方法贡献与独立实证 | 用户要求不能只有增量改动；当前O07固定参考/有界残差属于ReBRAC任务适配，研究门槛通过不完成本项，不称SOTA。需另提出可检验的方法问题、对应消融与新冻结评价；2026-09-16按新目标开展RL-DITR基底、近期方法依据与单seed实验；未验收 |
| M02c | ☐ 将已验证研究RL接入Core受控执行并完成产品动作/适用性合同验收 | O07独立研究CLI不是注册执行器；仍需合法来源、固定参考有效期、版本/动作/父预测绑定、失败与拒收、真实双模型联调及发布闸门验证，不降低已有产品要求 |
| M02 | ☐ 实现并验收真实 RL 的仿真基础输注任务 | 原阶段 5 及 02 的训练 TODO 逐项执行；独立评价环境、动作及失效合同，不以 fixture 代替；算法无收益保留负结果 |
| V01 | ☐ 验收完整用户任务与给药研究闭环 | P01–P16、M01/M02 必要项完成；同一虚拟患者多模态资料→追问→两真实模型→建议/解释→真实双审→发布→执行反馈→新评估，且攻击/故障被拦 |
| V02 | ☐ 完成新环境原生交付与数据恢复 | 承接原阶段 8 的打包/子进程/退出恢复；真实窗口和全通道链路、历史数据及卸载/更新约定，不只编译成功 |
| O01 | ☐ 接通 Observation 与受控进化 | 依原阶段 4/7 各细项验收；正式进化须在完整必要链路、两真实模型及各闸门通过后，按第 5 节公开真实数据划分和固定基线开展；外部故障不影响核心真相 |
| C01 | ☐ 制定并满足真实患者给药服务的准入要求 | 明确实际人群、药物/装置、用途、专家规则与审核、临床验证及适用监管要求；不能用仿真/软件测试代替，未完成不解锁真人给药 |

执行依赖：P01 → P02 → P03 → P04 → P06（最小会话/事实状态）→ P05 → P07…P16，每轮一个编号或其明确子项；先有事实与澄清状态，再验收主 Agent 恢复循环。当前数值模型继续占位。M01/M02 仅在对应模型工作获得继续指令时开展，但 V01 的真实双模型验收必须等二者完成。P09、M02、O01 是对既有细项的索引，不作为一次跨多步实现的许可；父项仅当全部必要子项验收后勾选。P16 的防护实现完成前，P11–P15 用合成或许可允许的去标识化资料，不接真实个人健康数据。任何新增输出在其渠道开放前必须复验 P08/P10 的发布约束。原表未被上述编号覆盖的论文、答辩和技术交底要求继续保留，排在用户产品基础之后。

P01 按以下子项验收，父项仅在两项均完成后勾选：

| 子项 | TODO | 验收范围 |
| --- | --- | --- |
| P01a | ☑️ 版本化研究输入与用途合同，接入 Core 冻结及执行前检查 | 46 项新增回归、全量 366 项通过；合成/仿真来源、治疗背景、实际输注历史、缺失及错域/错时序检查；旧库副本原行保留，默认执行阻断；仅 P01b 新增显式合成 fixture 配置。见 implementation/docs/basal-inputs.md |
| P01b | ☑️ 完整基础输注动作、用途配置与证据/时效绑定 | 新增 49 项，全量 415 项通过；显式合成 fixture 配置、确定性速率/时长/总量、消费者矩阵、配置内容与动作/父预测/输入/版本/期限全链绑定、实际用药与建议分离；独立 worker + HTTP 双审发布/读取及旧库兼容通过。真实模型配置为 null，留 M01/M02 核验。批次 21 新增 25 项、全量 440 项通过，补充 U/min↔U/h 受控等价转换、原输出保留和转换版本绑定，旧 v1 不升级。见 implementation/docs/basal-actions.md；待用户审核 |

P02 分为以下两项；只有全部验收后才勾选父项：

| 子项 | TODO | 验收依据 |
| --- | --- | --- |
| P02a | ☑️ 当前字段数据流、最小外发与按病例/快照/用途授权撤回 | 公开固定政策；本地/报告 Agent/双 reviewer/Observation 字段清单；默认拒绝，网关/工具/完成时重验，跨病例/角色/快照及重启检查；桌面明确授权。新增 28 项、全量 468 项通过，本地 HTTP/真实 SDK + 离线 mock 及原生构建通过；未实点原生窗口。见 implementation/docs/data-policy.md；用户已授权继续 P02b |
| P02b | ☑️ 保留/删除、SDK 留存、备份恢复与撤回后的生命周期合同 | 核对真实落盘副本和日志；保留/删除状态及外部供应商边界；备份恢复不复活旧许可，跨病例与审计正反验证；与 P16 完整保护衔接。批次 23 新增 28 项、全量 496 项通过，真实 SDK + 离线 mock 的落盘/删除及 HTTP 运行中删除、旧库兼容与原生构建通过。用户已授权继续 P03；未实点原生窗口，未声称旧备份/远端/物理擦除完成 |

### 7.3 怎样判断闭环完成

| 层次 | 完成条件 | 当前结论 |
| --- | --- | --- |
| Harness 工程基础 | 依赖、权限、作业、渲染、审核、发布的正反验证 | 已有大量实现；桌面全链仍有缺口，537 个工程测试不是整产品完成率 |
| 面向用户的产品任务 | 无需开发者操作，能输入/补资料、得到合规类型结果、理解失败并反馈；会话和数据全链可靠 | 未完成；对话、多模态、真正的主 Agent 与产品解释需落实 |
| 双模型给药研究闭环 | 两个实际模型在同一适用仿真任务中推理，动作影响后续状态，独立评价、解释与反馈可复现 | 未完成；当前数值产物是 fixture |
| 真实患者产品 | 针对明确用途证明输入/模型/输出技术性能及临床有效性，落实专家安全方案、必要人工介入、数据保护和适用准入 | 未完成；文档或 SDK 接通不构成此项证据 |

验收前冻结正常/缺失/故障/攻击病例、患者划分、评价方法和通过条件。除约定测试集上硬约束零违规，还记录有效任务完成率、误拒率、关键字段正确性、解释忠实与可理解性、超时/成本和反馈准确性，不能以全部拒答通过验收。实际临床用途须独立评估，参考 [IMDRF 的 SaMD 临床评价框架](https://www.imdrf.org/documents/software-medical-device-samd-clinical-evaluation)；该参考不替代具体地区的准入判断。

## 8. 实现时应查的官方依据

- [MCP 工具协议](https://modelcontextprotocol.io/specification/2025-06-18/server/tools)：结构和错误语义；不是推理真实性证明。
- [Claude Agent SDK hooks](https://code.claude.com/docs/en/agent-sdk/hooks)、[subagents](https://code.claude.com/docs/en/agent-sdk/subagents)、[安全部署](https://code.claude.com/docs/en/agent-sdk/secure-deployment)：事件/工具约束与部署隔离；版本行为需实际验证。
- [DeepSeek Anthropic 兼容接口](https://api-docs.deepseek.com/guides/anthropic_api/)、[视觉能力](https://api-docs.deepseek.com/guides/vision/)：具体模型及字段兼容性，不由 SDK 名称推断。

本次 v6 仅完成产品方案复审与文档修订。既有软件实现和测试以 `工作记录.md` 最新批次为准；真实数值模型与完整用户产品仍未完成。
