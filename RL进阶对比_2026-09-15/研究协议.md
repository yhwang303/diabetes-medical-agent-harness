# 持续目标与预先定义的比较协议

## 用户目标与当前进度

目标保持为：训练可验收的研究用RL模型，完成2–3个较新基线的公平对比、具论文依据的实质改进、多seed评价、全部尝试HTML与论文式大表。只有资料、代码、烟测或患者模型成功不能标记目标完成。不能预设我方获胜。

本轮新指令允许修改研究模型、损失与risk；不自动修改产品可信规则、医学语义、安全renderer或已冻结证据权限。旧80%方向比例只保留历史诊断，不继续作为策略研究的一票否决；新增排序先验后，同类方向测试只算先验一致性。

## 比较对象与论文依据

- ReBRAC（NeurIPS2023）与FQL（ICML2025），准确公式/作者commit见baseline_sources.md。
- BC为行为学习参照，不计作较新RL算法。
- 现有RL-DITR适配作为前期患者模型参照；我方后续主线依据Hybrid² Neural ODE Causal Modeling（ICML2024），有向机理状态图与预测/干预排序混合学习，进一步开展受约束策略和规划。具体实现须在运行前追加，不凭标题冒充原文复现。

## 数据与动作公平性

保留Loop v2全部225训练患者/1,653,421起点、55验证患者/424,866起点，72×22历史包含food/exercise值、mask、年龄及年龄可知性。基线直接flatten为1584维，各自学习，未使用我方预训练编码器。

所有起点进actor的行为学习；主比较的TD损失均采用相同bootstrap_valid且有实际next_action的交集。其他起点保留，不将未知尾部target设为r或伪造下一动作0。掩码只涉及合法监督，不以模态存在性筛数据。FQL不需要next_action，后续可补自然最大合法集合结果并单列数据量。

原动作全范围核验[0,20] U/h；连续映射a_norm=a_Uh/10−1，无量化、无原动作裁剪。ReBRAC初始target noise=.02、clip=.05归一化单位，即.2/.5 U/h，作为公开任务适配，避免默认噪声在本任务对应2/5 U/h。其他超参保留作者主要默认；batch初始统一256。每方法等额调参，不能只给我方更多训练/验证。

研究奖励初始沿用冻结v2：paper_status_score(next_glucose)/12；γ=.9^(1/12)，5分钟为一步。没有为我方单独换reward。若后续改risk，应追加来源/版本并同合同重跑比较，保留原结果。

## 训练与实现

PyTorch重实现，不称JAX字节级复现。ReBRAC采用3×256/ReLU、激活后critic LayerNorm、作者小输出层初始化；FQL采用4×512/GELU(tanh近似)、激活后critic LayerNorm、Glorot初始化；LN eps=1e-6。ReBRAC Polyak在本次optimizer更新后执行，是标准形式，与作者actor目标的一个更新时序细节不同。FQL各损失先在同一参数快照计算梯度后更新。逆mean绝对Q归一化加1e-6下限防除零。

先1000更新测吞吐与工程错误，再每方法50k pilot看趋势；pilot不叫充分基线。之后相同预算扩至200k–1M，按开发仿真指标和收敛趋势决定，最终至少3训练seed。完整保存实际更新数、访问次数、完整数据遍历次数、等价遍历次数、耗时、源码/数据SHA、checkpoint与失败。日志中epoch/完整pass是全起点遍历一次，不用作者1000步日志周期偷换。

## 独立评价与验收（在产生闭环成绩前冻结）

模拟器固定官方simglucose commit a6f6777586c860457a2a1e6cce6d0bd3fd33d1cc，不修改机理患者参数迎合策略。5分钟观测/决策；统一U/h→U/min转换、泵实际交付核验、输入可得性和共同的餐时bolus协议。policy不得读取env.info中的真实隐状态或未来事件。原生模拟器不能验证运动效应，必须明确其边界。

开发患者adult001–004用于方法选择；adult005–010留作最终独立患者评价，最终情景seed不参与选择。训练依然来自Loop，任何额外仿真训练要另建明确实验行，不能与真实数据训练混称。餐食时序/碳水、初始预热、共同bolus、传感器噪声、情景时长/seed等须在首次评价前写成具体JSON合同并保留hash。

主表报告TIR、TBR<70/<54、TAR>180、平均血糖/波动、同一risk积分、基础胰岛素总量、动作投影/失效、参数数与GPU时间。按相同患者/情景配对，报告跨训练seed均值±SD及患者聚类差值区间。预测指标另外一panel，不和策略疗效混表。

研究验收应基于独立闭环实测：相对合理参考策略改善预定主要指标、低血糖风险不明显恶化、跨seed稳定且边界行为可解释。具体统计容差在首次闭环评价前补充；不能看完成绩后挑宽松阈值。研究验收不等同真实患者、独立双模型或Harness产品准入；最终仍要完成受控执行、来源/适用域与证据绑定核验。


## 20:13 短测后的基线任务适配

S01均通过工程损失/梯度/225患者对齐检查。ReBRAC约213更新/秒，FQL约105更新/秒；这只是吞吐。ReBRAC在1000更新验证时动作全部饱和0，原样保留。训练动作均值0.7860178 U/h，归一化均值−0.9213982；默认tanh零偏置对应10 U/h，远离典型数据。下一S02将所有方法actor输出偏置按train-only均值初始化（FQL flow速度最后偏置也用该均值），ReBRAC/BC actor lr降1e-4、critic lr3e-4、ReBRAC beta_actor=10，FQL其他初值不改。此为公开的适配组合，不能用它单独证明饱和确切成因；初始化/lr/正则消融若需要另列。两个原S01与所有失败保留。

## 20:26 独立仿真合同冻结

首次闭环评价前冻结 `simulation_contract_v1.json` 及 SHA。主要指标为按患者等权的 TIR；BC改善至少2个百分点、相对nominal及最强较新RL基线不劣于2个百分点；低血糖相对BC/nominal增加不超过1个百分点（<70）/0.5个百分点（<54），无提前终止。3训练seed中至少2个改善BC，并报告患者聚类区间。它们是本研究的预设比较容差，不是医学准入标准。开发001–004、封存005–010，各3情景seed，3天/6小时预热，餐时bolus系数1.0/0.8/1.2所有方法共用。静态nominal基线和共同餐时模块使用公开患者参数，学习策略本身不可见这些参数。所有结果无论成败均保留。

## 20:33 有向图患者模型首版（运行前记录）

独立实现H²NCM思路：2层128宽LSTM编码72×22历史，初始化7潜状态；glucose初态为当前标准化观测，其他为学习状态。7个独立64宽tanh节点MLP按已记录父节点同步5分钟残差更新，胰岛素至少4步才通过通路影响glucose；潜状态不是已标定的生理量。未来basal与记录的bolus/carb/exercise值+mask用于事后条件监督，未知不写成真实无事件。完整1,653,421起点进训练、12步目标以原资格mask；不要求都有60分钟记录。G01 alpha=0；G02 alpha=.05，混合归一化MSE与论文式3干预CE，phi=1、同历史/共同事件，候选为首个基础率的0.5/1/1.5倍+0.05并限原动作合同。排名仅从20分钟起算；标签是先验，方向指标不作为独立成功。两组各3epoch、lr3e-4、batch256、梯度范数上限10且记录原范数。未来将用训练来源的共同事件情景与独立策略仿真；本次患者模型训练不等同RL策略完成。

## 20:41 图模型参与真实RL的首个策略实验（运行前记录）

依据 H²NCM 的结构/排序、Amos等L4DC2021的可微短期模型回报参与策略更新思想（https://proceedings.mlr.press/v144/amos21a.html），以及MOPO关于模型不确定性的惩罚思想，独立实现本任务的模型辅助ReBRAC。它不是TD-MPC2或SVG原算法复现，也不声称论文已证明此组合有效。

保持真实离线ReBRAC的TD损失、奖励、gamma和行为正则。actor增加 λ[-R_model +0.1×模型分歧]：从观测历史编码到两个冻结图模型，候选基础率在12个5分钟步保持，按同一paper_status_score与gamma计算短期回报；G01/G02的预测SD仅作不同模型分歧代理，不称校准置信度。没有terminal价值扩展，不将该正则称为完整SVG回报估计。lambda先1和3两组，不改变独立评价指标。

未来共同事件来自train-only库8192条完整60分钟联合bolus/carb/exercise值+mask序列；按当前CGM、30分钟趋势、上小时基础率与事件记录数找到16个近邻，每批抽2个。完整60分钟资格只限定情景库来源，全1,653,421个actor起点保持。库不是因果事件生成器，未记录仍未知；策略更新不读取该历史的真实未来事件或验证/模拟器未来。两冻结模型和事件库SHA写入策略来源。

H00是100更新工程短测；H01/H02从同一ReBRAC50k的actor/critic/targets/optimizer/RNG显式warm-start，各再50k至100k，与原ReBRAC100k比较。无模型baseline、λ=0精确同参数更新检查、G01/G02及后续模态/图结构消融用于判断真正贡献。若无独立闭环改善，这条方法不会被记为成功；额外患者模型计算成本单独报告。

## 20:48 两基线等额行为约束调参

50k开发迁移结果失效后，R21将ReBRAC beta_actor从10改100；R22将FQL distillation alpha从10改100。其余相同数据、seed、初始化、lr与50k预算。各新增一个50k试验，不使用封存患者挑参。保留原50k/200k与100k中间结果，不能只报告改进成功的一组。ReBRAC100k在同12条开发情景TIR16.5930%、TBR70为6.4289%、TBR54为5.1031%、1次终止；它虽提高TIR但没有修复风险。

## 20:56 独立幅度诊断后的定性约束损失对照

独立模拟器dev4成人×7起点的同状态复制干预已完成；零干预分支逐输出完全一致、共同事件跨剂量相同。G02的+1U/h在60分钟预测变化中位数−0.1853，模拟器−0.03610 mmol/L；+0.1U/h分别−0.03721/−0.003630。这批数据只作开发诊断，不能称最终独立测试，更不能拿方向100%宣布通过。

G03保持图结构和3epoch/全Loop训练，改变先验损失：依据Liu等NeurIPS2020《Certified Monotonic Neural Networks》公式11的违背单调方向才惩罚的平方hinge思想（https://proceedings.neurips.cc/paper/2020/hash/b139aeda1c2914e3b579aafd3ceeb1bd-Abstract.html），用相邻候选动作的有限差分斜率代替导数，penalty=mean(ReLU((g_high−g_low)/(a_high−a_low))²)，相同动作的边排除。它是本任务的有限差分适配，没有运行作者的MILP认证，不称全域单调保证。正确方向即零惩罚，不通过CE持续拉大血糖间距。总loss=0.9×归一化MSE+10×hinge；全部actor起点/未来mask/输入不变。G01/G02及CE幅度失准结果保留。实际作用仍需独立幅度和策略评价。

## 21:04 个体参考与残差策略结构对照（运行前记录）

观察到BC/ReBRAC/FQL闭环基础率持续漂移且患者间差异大；强BC的ReBRAC50k平均TIR81.9129%、TBR54=0、0终止，但adult003仅53.37%TIR。H01图辅助lambda1反而TIR6.3447%、6终止，保留负结果，不称我方优于基线。

依据Johannink等ICRA2019《Residual Reinforcement Learning for Robot Control》（https://arxiv.org/abs/1812.03201），提出本任务的“固定历史参考+学习残差”结构适配。该论文是机器人任务，未证明糖尿病疗效；这里独立实现并单列新增信息/结构对照。每个患者参考取第一合格决策前72个5分钟历史格实际基础输注量均值×12，此后固定。模拟器参考只从共同6小时预热实际已交付基础量计算，不访问官方患者参数或未来数据。保留原72×22历史，包括food/exercise，另追加一个归一化参考scalar（1585维总输入）；全部225训练患者/1,653,421起点仍保留。参考来源不得晚于最早决策。它只是历史输注描述，不是已确认临床基础率；长期变化需模型学习残差，不能将参考视为永远有效的治疗处方。

actor输出 clip(reference_Uh + 2×network_residual,0,20) U/h；最后一层零初始化，因此初始残差为零。原动作/TD目标仍使用既有无损U/h映射，原始动作不裁剪；只有实际policy输出执行原合同界限。R30/R31/R32分别为BC/ReBRAC/FQL，所有方法共享相同参考信息和50k预算；ReBRAC beta_actor=100，FQL alpha=100，其他与对应试验相同。FQL的一步actor因此改为有参考的有界输出，须称结构适配，原FQL结果保留。此组不能与原22维组混称同输入公平排名。

仿真每个患者新建policy实例，参考不能跨患者复用；批量离线推理显式提供参考列。未来Harness接入需把这个历史参考的患者/时间/来源绑定进输入合同，当前尚未改产品合同。必须比较参考自身（共同预热下近似nominal）、残差BC和残差RL，以及是否真正改善控制；不能用一个近似固定参考的网络冒充RL贡献或宣称成功。


## 21:16 已保存权重的开发选择与参考对照

R31个人参考ReBRAC50k在12条首seed开发轨迹TIR92.0770%、TBR70为0.1263%、TBR54=0、0终止；相对nominal仍低3.2197个百分点。R30同信息BC为26.5152%、2终止。R22强约束FQL为30.7660%、0终止；H02图辅助lambda3为12.7210%、TBR54=3.8194%，不通过。均保留。

追加既有R21强BC ReBRAC和R31参考ReBRAC的10k/20k/30k/40k检查点，在相同4患者×3101×3bolus条件评价；不新增训练更新，不触碰封存患者。与已测50k一起按冻结的无终止、相对低血糖容差、TIR顺序选择，随后需补齐全部开发3情景seed及至少3训练seed验证。其他基线最终须获得相同检查点选择机会，不能把本轮选择预算优势写成算法优势。

C01是零残差、无学习的可见参考控制组：取已训练R31导出的网络并将最后一层权重/偏置明确归零，使输出仅等于共同预热历史参考；记录父导出SHA但训练更新计0，不冒充RL。参考重建与显式列逐数一致，后续历史不覆盖参考，不同policy实例无跨患者复用，torch/NumPy输出一致已验证。


## 用户最新范围调整：外部baseline仅一个训练seed

用户要求外部baseline用一个seed得到可比结果，重点优化我方模型。相同患者数据、预处理和评价情景保持；新增输入及训练预算差异明示。不再为外部baseline扩展3训练seed，原多seed要求仅保留于拟验收的我方候选稳定性验证。原冻结JSON保持原样供追溯，此条是用户明确调整的比较预算；性能、低血糖及失败容差不因此降低。已生成但尚未提交的8个额外baseline seed配置移入configs/not_run_user_scope_change，不计尝试或训练。R31参考残差候选两个新增seed继续用于自身稳定性检查；它是基于ReBRAC的任务结构适配，不能冒称新算法论文成果。已在运行的单seed实验按实际结果保留，不再追加外部baseline搜索预算。


## 21:22 我方有界残差结构（训练与评价前登记）

R31在10k/20k/30k/40k均曾出现开发终止，50k才较好；当前不能证明稳定性。依据Residual Reinforcement Learning for Robot Control（Johannink等，ICRA2019，https://arxiv.org/abs/1812.03201）的参考加残差结构，以及BEAR（Kumar等NeurIPS2019，https://proceedings.neurips.cc/paper/2019/hash/c2073ffa77b5357a498057413bb09d3a-Abstract.html）关于离线动作外推与bootstrap误差的分析，增加任务特定有界输出：clip(reference + L*tanh(2*f(history,reference)/L),0,20) U/h。L分别0.25/0.5 U/h，是开发超参数，不是临床安全限值，也不保证位于数据支持域；不是BEAR算法复现。零点局部斜率与原2*f一致，零初始化仍返回可见参考。

O01/O02为两个边界的真实ReBRAC残差RL，O03/O04为对应去RL的行为学习消融；同seed260915、同225患者/1,653,421起点、同50k更新。critic仍用完整原始日志动作，actor行为loss也保留原始目标，不把区间外目标裁剪、不丢样本。仅policy输出被限制到参考附近的子空间；这种限制可能无法处理较大基础需求变化，必须报告该适用边界。新增两个无RL组用于识别我方结构中的RL贡献，不是给外部baseline追加seed或广泛调参。所有默认旧模型公式保持，旧导出及权重不改；评价阈值不改。


## 21:34 有界模型的行为模仿与价值权重对照（运行前登记）

O01在有界输出下保留全部原始目标，训练行为误差约0.0118（归一化MSE），beta_actor100对应约1.18的模仿项；Q归一化项量级约1。单看loss量级不证明梯度主导，但支持检验权重敏感性。O05/O06分别保持O01/O02的±0.25/±0.5有界结构，将beta_actor100改为10；同seed260915、同全量Loop数据、同50k更新、同reward和gamma、同动作合同及评价规则。两组从头训练；与已运行beta100构成2×2小范围对照，依据现有ReBRAC的行为与价值目标公式，不宣称新算法。没有增加外部baseline seeds或调参预算。


## 21:38 调整范围的中间值与同结构消融（运行前登记）

O01(±0.25)在开发TIR92.2243%、TBR70 0.1368%；O02(±0.5)TIR95.0231%、TBR70 1.7782%。因此O07选择中间范围±0.4 U/h，其他与O02相同（beta_actor100、同seed/full Loop/50k）；O08是同范围去RL对照。半径来自这两端的开发权衡，不是论文给定的医学参数，也不使用封存患者。0.4的目标是改善TIR与低血糖权衡，不修改评价阈值。O03(±0.25去RL)12条完整方法面板TIR85.4272%，低于对应RL92.2243%，但整D16阶段尚在完成第二方法，整体完成后回收报告。


## 21:46 通过首情景数值筛查后的固定配置扩展

O07(±0.4，beta100，50k，seed260915)在12条开发轨迹TIR95.0968%、TBR70 0.2210%、TBR54=0、0终止；相对nominal TIR−0.1999个百分点，首次同时满足本批数值容差。不是最终验收，也未使用封存患者。

固定相同50k配置训练seed260916/260917，不改变数据、奖励或半径。D23补齐4开发患者×3既有情景seed×3bolus系数共36轨迹/方法：nominal、原BC、强行为约束ReBRAC、强蒸馏FQL、参考FQL、O07及同结构去RL，共7方法。所有外部baseline仍只用训练seed260915，不追加训练；情景seed是不同餐食/传感噪声评价，不是更多训练seed。3101已评价情景重复用于同一完整面板及复现核对，不能把重复当额外独立样本。

原v1性能数字与封存患者/情景划分保持。v2_reference协议只明确已在21:04/21:22登记的参考输入/输出限制以及用户最新baseline一训练seed范围，并明确它是在开发探索后、封存评价前形成。父SHA及全部性能阈值逐字段一致已核验。


## 用户最新指令：所有模型均只训练一个seed（21:48）

停止O07额外seed训练的进程组，远端核验所有train_rl进程已消失。seed260916最后日志为10750步，保留部分权重/日志并写interruption.json；seed260917未启动。已完成的历史R31多seed负结果保留，不删除历史，但后续主比较固定seed260915，不再要求或启动任何模型的额外训练seed。

v3_single_seed仅将training_seeds_min、seeds_with_TIR_improvement_vs_BC_min由3/2改为1/1以符合用户明确指令。TIR、低血糖、终止、患者区间等性能容差和封存划分均保持。后续不能声称多训练seed稳定性；场景seed仍代表共同的餐食/传感噪声测试，不构成重新训练。D23所有方法用同一训练seed权重、同一批完整开发情景。


## 2026-09-15 22:01 后续执行记录：完整开发与冻结封存

D23全部252轨迹完成；O07固定参考±0.4 U/h、beta100、seed260915、50k通过预设完整开发门槛。TIR95.1284%，相对nominal−0.5366pp、ReBRAC+12.7455pp、BC+44.7320pp、同结构去RL+16.6456pp；TBR70 0.2771%、TBR54 0、0/36终止。开发患者bootstrap相对BC区间[38.5347,52.0027]pp。48条重跑pilot逐步记录一致，不能增加独立样本数。

已在首次使用封存adult005–010之前，由final_evaluation.py freeze写configs/T01_frozen_sealed_comparison.json，SHA44e8c1fe19a8236ee048e87196d7f31191ba6665cadad319419ee08696b9b137。其封存前时间、权重/源码/协议/开发选择证据SHA可核验。7方法均保留D23同一权重，三个封存情景seed73101–73103、三种bolus系数、6患者、3天，共378轨迹。独立入口验证固定计划，原开发入口封存阻断保持。此批不会根据封存成绩改参数再重称独立测试。

单seed仅是用户指定的训练次数，模拟情景seed继续成对一致。主比较同50k更新，但我方结构与参数探索累计预算更多；这是任务适配后系统比较，不是等额搜索的算法普遍排名。当前模型输出以固定历史参考为中心，参考可能继承共同nominal预热优势；与纯参考和同结构去RL共同展示，不把全部收益归因于RL。

独立研究推理包用于交付可复现权重，不修改Core注册、来源信任、发布权限或医学规则。最终研究判定以T01完整结果为准，不以开发结果或推理测试代替。
