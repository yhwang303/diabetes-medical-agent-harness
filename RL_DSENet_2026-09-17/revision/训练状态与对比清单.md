# 本轮最终状态

当前本方法 P03 DSENet + D05参考/响应 + D06策略，seed260915，已完成既定训练。结构图在 `figures/architecture_actual.{svg,pdf,png}`；结构与训练预算见 `结构与训练状态.md`。

新增 TD3+BC (NeurIPS2021)、IQL (ICLR2022)、LOM (ICLR2025)、GFP (ICLR2026) 均已完成单seed训练与评估。旧BC/ReBRAC/FQL权重复用；RL-DITR取原R03连续任务适配，不采用后来的内部增量版本。

正文表8个外部方法、ours和constant-basal，共10行，每方法60场景；共同范围主表、原生范围附表均已完成。1080条不同轨迹（新增780、复用300）全部重算通过，1200表内条目包含主附表复用的ours和constant-basal，不假称1200次独立新运行。

报告入口：`论文正文对比报告.html`。正文双栏表：`paper_tables/main_comparison_compact.tex`；完整表：`paper_tables/main_comparison.csv`。原生附表、配对统计、风险表同时保留。

TIR ours97.08%、FQL89.78%，但TBR70 ours1.55%、FQL0.66%，TBR54 ours0.57%、FQL0.25%。本方法额外仿真信息已以L+S列出，不能称严格同训练数据优势；单seed、已曝光患者、个体低糖问题及算法任务适配均需保留。
