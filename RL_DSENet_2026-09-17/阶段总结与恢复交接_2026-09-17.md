# 阶段总结与恢复交接 · 2026-09-17

## 当前结论

本轮已完成DSENet/Loop适配、四个patch超参数初筛、单seed260915预测/world/策略训练、32位Loop留出患者预测和12方法×60条冻结新场景控制评价。最终仅策略模型平均BG TIR97.0770%、TBR70 1.5488%、TBR54 0.5661%，0/60终止，覆盖100%。

这不是全面胜出或可靠控制器：相对固定基础率TIR增加1.9003pp，同时TBR70增加0.5661pp；相对同world规划器TBR70减少0.5366pp，而TIR差值区间含零。旧最强有界RL的TIR95.7618%，新模型优势点估计+1.3152pp，95%区间−0.4146～3.6974，不能称显著胜过该旧参考。adult009的TBR70 12.3948%、TBR54 5.6608%，6次连续至少120分钟低于54的事件。这个结果必须保留，不能只引用总体平均值。

## 模型与依赖

- 最终预测：`results/P03_g12s3_l2s1/best.pt`，SHA256 `c01610104632a967b4de5eaafe59511a67e3b4d0d34954fab5bc7341fb8d19b4`。
- 最终world：`results/D05_selected_world/world.pt`，SHA256 `deddaa51d00a92feb61570b1fe1ffad7c20dd85b33d74a772c62eaf0ea60f1ae`。
- 最终policy：`results/D06_selected_policy/policy.pt`，SHA256 `348caaf3d3474d34cd9091dfa90aeba31305499d2e34d98279999bfcfdb16913`。
- DSENet上游560712e7d8e01f25a5dad3262da786f4a0ae52de；global length12/stride3、local length2/stride1。主结构保留，缺失/尺度/local mask/Fusion细节修正见README及CUDA checks。
- DSENet316,782参数；冻结H02历史模块9,621,295（存储含未参与输出的旧头），参考45,360、响应17,480、策略40,199。不得把DSENet参数量当整个系统参数量。
- `.venv-native`用于CUDA训练/推理；`.venv`用于simglucose及原始指标精确重算。两环境风险函数可能有约1e−15浮点差，不能在不同NumPy环境要求浮点字典逐位一致。

## 完成的验证

1. 保留全部225人/1,653,421训练起点和原稀疏辅助特征；每预测配置3完整epoch，新策略1完整epoch。
2. 6个patch配置完整比较；按固定128起点/验证患者的指标选P03，没有用final场景或sealed_test选型。P03只比D01该选择分数低约0.08mg/dL，全量均值D01反而约低0.02，不能夸大patch收益。
3. 真正执行参考分支的监督、零干预恒等、未来动作因果、效应误差和regret门槛、旧头不参与、新DSENet影响utility、world冻结hash全部通过。
4. 全部720条控制原始记录在仿真环境重新计算，指标与原输出精确一致；方法间场景匹配，旧baseline权重不变且未重训。
5. 留出预测RMSE30/60/120/240为19.5546/33.2038/46.1269/58.8134mg/dL；有效患者n32/32/31/31；全部优于Persistence，120min低糖子集略有退化。
6. 1,037个最终证据文件下载hash验证通过。archive SHA256 `644cbe8903d85a399310960e2eed66f9301a4f748eb167bc7ef39c2f3a8bc3ec`。
7. 模型包34个文件，在隔离目录CUDA加载，与原目录动作输出逐值一致；本地包及内部文件hash一致。仅策略单病例推理中位数5.24ms（RTX4090，不含JSON/仿真）。

## 交付入口

- `训练审核报告.html`：完整中文报告，含前置个体风险提示与所有实验表。
- `paper_tables/control_final.tex` / `.csv`：12方法论文主表。LaTeX需booktabs，限定条件不能删。
- `paper_tables/forecast_heldout.tex`：留出患者预测表。
- `paper_tables/results_paragraph_en.md`：含风险和统计限制的英文结果段落。
- `delivery/DSENet_RL_single_seed_model.tar.gz`：完整研究推理依赖包。
- `delivery/final_evidence.tar.gz` / manifest：原始训练与评测证据。
- `审计落实清单.md`、`实验协议.md`、`README.md`：审计映射与复现说明。

## 远端状态与恢复规则

远端根为`/root/autodl-tmp/diabetes-agent`。三条最终评价队列baselines/ditr/ours及最终analysis-simruntime归档作业均已退出0；所有训练已结束，不需要续训R08或重复baseline。恢复时先核对作业状态和文件hash，不覆盖任何已有results目录。密码不写入交接；连接使用项目现有安全配置。

## 尚未达到的目标与下一步

目前仅有预测和特定控制指标的研究收益，尚不能宣称可靠控制器、全面优势、临床安全或会议录用水平。10个虚拟成人早已曝光；新场景不等于未见患者；单seed区间不含训练随机性；跨系统训练监督与动作范围不同。

如果继续提升可靠性，先在开发阶段增加有监督的更宽减量/暂停候选、低糖恢复及不同基础需求场景；检验响应支持、参考校准与风险目标后再重训，保持同后端对照。当前最终场景一旦用于下一轮开发，就必须改称已曝光数据并用另一组事前冻结的新场景评价。不能根据本次final结果改完模型，再把同一面板称一次性封存结果。

Core/Harness与医学规则未改，真实模型受控执行器未注册；产品父项保持未完成。HTML做了结构、链接和关键数值核对；浏览器本地file URL被策略拒绝，未绕过，故没有声称完成视觉预览；本机无TeX编译器，仅做LaTeX结构检查。
