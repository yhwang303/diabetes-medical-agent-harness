# 产品闭环审查与验收

日期：2026-09-10。对象：01/02 两份执行方案、当前实现、工作记录及相关一手资料。本文记录 v6 的判断依据；执行与勾选仍以 [总体方案](01_项目总体方案.md) 第 7 节为准，不维护另一套完成状态。

## 1. 结论

**v5 有较扎实的 Harness 工程设计，但不是完整的用户产品方案。按旧 TODO 原样做完，不能证明能交付用户要求的医疗 Agent。** 主要原因是将研究者操作的工作台作为最终成品，将数值模型固定编排后再调用报告 Agent 作为主流程；多模态、自然对话、可理解的胰岛素建议、实际用药反馈及真实患者准入缺少贯通的合同和验收。

当前无需推倒 Core、worker、SDK 或审核版本绑定。应保留这些工程基础，把主 Agent 放到任务入口，给它受控业务工具，并完善输入、动作、解释、输出和反馈的产品合同。本轮只改方案，不声称新能力已实现。

用户最新约束：最终体验为多模态生理数据输入，获得胰岛素建议及自然语言建议；Agent 主导任务决策，Harness 控制执行和发布。两个模型的有效推理结果缺一不可。研究仅使用公开数据集和公开仿真资源；RL-DITR 只借鉴方法，不复刻其私有医院队列、临床任务或实验。

## 2. 从用户任务倒推必要条件

| 必须回答的问题 | 产品要求 | 当前缺口 / v6 对应任务 |
| --- | --- | --- |
| 给谁、针对什么动作建议？ | 有明确人群、胰岛素动作、用途、输入及时间范围 | 原“给药”过泛；补 P01，首个研究任务选成人 T1D 基础输注 |
| 用户怎样提供可用信息？ | 对话、上传、字段核对、必要追问；不要求手写 JSON | 当前只有结构化工程输入；补 P06、P11–P13 |
| 谁决定下一步？ | Agent 根据任务和结构化反馈选择工具，Core 强制检查合法性 | 当前先固定 execute_models，再 reporter；补 P03–P05 |
| 两份结果怎样证明可用？ | 同病例/快照/时点、合法生产者/版本、适用域、单位、父依赖与时效 | 已有工程校验，但真实模型/域验证未完成；保留 M01/M02 |
| 用户实际读到什么？ | 明确动作卡和有证据的自然语言解释，而非纯工程字段 | 当前仅章节排序与 fixture 模板；补 P09/P10/P13 |
| 所有出口是否一致？ | 最终同版本双审，所有个体化内容经唯一发布 | 后端基础已有，桌面双审/全通道未齐；补 P07/P08 并持续复验 |
| 不能给建议时怎么办？ | 追问、无剂量说明、风险升级；不能卡死或猜剂量 | 现有状态码不等于用户失败体验；补 P14 |
| 建议之后是否形成下一轮？ | 记录实际行为/测量、核验来源、产生新快照及评估 | 目前无实际用药反馈合同；补 P15 |
| 健康资料是否受控？ | 外发授权、最小化、隔离、存储与生命周期防护 | 现有认证/路径控制不足以覆盖；补 P02/P16 |
| 怎样证明不是只有演示？ | 正常任务完成与故障阻断均验收；模型/医学/产品分别取证 | 补 V01/V02/C01；保留 Observation 与进化，不抢占用户主链 |

## 3. 当前实现的证据与界限

- [agent_tasks.py](implementation/src/medical_harness/agent_tasks.py) 的 `_work` 先 `core.execute_models`，再 `reporter.generate`。这是工程报告任务，并非主 Agent 自主选择预测/RL 的循环。
- [sdk_worker.py](implementation/src/medical_harness/sdk_worker.py) 已用 Claude Agent SDK 的 `create_sdk_mcp_server`；报告角色有合同/提案/状态三个操作，审核角色有读取/提交两个操作。烟测的 prediction_probe 不能当作完整的受控预测业务接口。
- [contracts.py](implementation/src/medical_harness/contracts.py) 的 SnapshotInput 主要是成人 T1D 标记与 CGM 网格；没有完整实际胰岛素历史、治疗配置、多模态事实和反馈合同。
- [render.py](implementation/src/medical_harness/render.py) 的模板为 `fixture-template-v1`，产物为 `EngineeringFixtureReport`。正文不接受自由模型文本，能防一类越权，但当前章节选择不是用户需要的自然语言建议能力。
- 真实 SDK/Flash 报告与两个 reviewer 已分别联调；[桌面说明](implementation/docs/desktop-agent.md) 和批次 16 记录显示桌面任务止于待审草稿，真实双审调度未接入。该批次两次任务成功、一次取消，审核/发布为零，不能称桌面完整医疗报告链已打通。
- 最近软件基线为 320 项工程回归通过。本文沿用该记录且复核相关源码，没有重新执行测试，也没有证明真实预测/RL、医学规则、RAG、多模态或临床能力已完成。

## 4. 架构判断：Agent 决策，Core 审批执行与发布

采用 **用户 → SDK 主 Agent → 受控 MCP → Core → 独立模型 worker**。工具结果回到 Agent，驱动追问、下一步请求、停止或解释；Core 在工具入口、产物验收、下游消费和发布时执行不可绕过的约束。这样既保留 Agent 的规划能力，也避免工具数量增加造成裸模型或自由输出旁路。

Agent 可以决定是否需要澄清、何时请求模型、选择哪些获准解释证据，以及是否停止。它不能选择跳过任何必需证据、把不合格剂量改成另一剂量，或自审自发。状态机描述合法依赖，并不要求把所有步骤写成无条件串行脚本。

两个模型都返回内容只是必要条件的一部分，还必须有效、适用、同源且未失效；两个模型都正确执行，也不等于建议医学上正确。尤其预测器只观察历史时，其曲线不能标注为“采用本次 RL 剂量后必然出现的血糖”，也不能独立比较不同剂量的后果。

SDK 支持 MCP 工具；MCP 规范要求实现者处理输入校验和访问控制，协议本身不提供本项目的临床证据或发布权限。[SDK MCP 文档](https://code.claude.com/docs/en/agent-sdk/mcp)；[MCP 工具规范](https://modelcontextprotocol.io/specification/2025-06-18/server/tools)。因此，“执行器在 SDK 外”与“由 Agent 通过 MCP 调用”并不冲突。

## 5. 胰岛素任务：依据公开资源选择，不复刻论文

本轮选择成人 T1D 基础输注作为首个研究任务，具体资源及备选比较见 [02 第 1.1 节](02_RL动态给药模型方案.md#11-首版胰岛素任务选择与产品衔接)。该选择是当前可实现、可验证性的判断，仍需核验现有预测器的适用域；若不兼容，应显式适配或重新评估，不能假装两模型已能合作。

公开 simglucose 有成人 T1D 虚拟患者，所核对的 Gym wrapper 将动作送入基础输注并令 bolus 为零，泵源码以 U/min 接收速率。这提供动作与后果的实验条件；不证明真实注射剂量或临床有效性。[项目说明](https://github.com/jxx123/simglucose)；[固定版本 wrapper](https://github.com/jxx123/simglucose/blob/a6f6777586c860457a2a1e6cce6d0bd3fd33d1cc/simglucose/envs/simglucose_gym_env.py)；[固定版本泵实现](https://github.com/jxx123/simglucose/blob/a6f6777586c860457a2a1e6cce6d0bd3fd33d1cc/simglucose/actuator/pump.py)。

DCLP3 和 Ohio 是可按条款申请的公开资源，本次没有取得或下载队列；可计划用于历史资料审计和预测适配。CGMacros 的多模态资料不能替代 T1D 给药训练或评价。不能把不同公开数据集的人物和模态硬拼成同一患者。[DCLP3 入口及条款](https://public.jaeb.org/dataset/573)；[Ohio 作者取得说明](https://webpages.charlotte.edu/rbunescu/ohiot1dm.html)；[CGMacros 数据说明](https://physionet.org/content/cgmacros/1.0.0/)。

本地 RL-DITR PDF 共 30 页。本次读取相关文字，并渲染核对第 2、14 页：论文采用住院 T2D 队列和七时点特征；可行性试验由医生确认治疗，CGM 在该试验中用于评估而不影响治疗；原个体医院数据并非公开可得。这些事实只用于限制论文证据的外推，不构成本项目照搬实验的理由。[本地论文](参考论文/RL-DITR_NatureMedicine_2023.pdf)；[论文 DOI](https://doi.org/10.1038/s41591-023-02552-9)。

可借鉴的是历史表征、动作条件状态转移、策略学习、行为约束及独立评价思想；本项目自行设计公开资源下的任务、基线和消融。不能仅因为原论文使用监督信号就称本项目策略得到安全保证。作者模型样例也不等于本项目所需数据和验证已经齐备。

## 6. 对抗审查：最容易“看起来闭环”的假成功

| 假成功 | 必须拦截或证明的事实 |
| --- | --- |
| SDK 已接，所以完整 Agent 已完成 | 查看实际谁发起模型请求，验证追问与恢复的真实 SDK 工具轨迹 |
| 两模型都有 JSON，所以可以继续 | 校验作业身份、当前快照、父依赖、适用域及有效期，业务失败不能算产物 |
| UI 上传了图片，所以多模态已完成 | 抽取字段有来源和核验，并进入明确允许的消费者；不同人群不能假配对 |
| 数值没改，但解释写成“立即注射” | 审核动作类型、时间、条件、单位和频率的语义，不只比较数字 |
| 给出预测曲线，所以证明剂量有效 | 历史条件预测不是动作反事实，报告不得制造因果含义 |
| 审核前先流式展示，最终再撤回 | 用户可见的所有渠道在发布前没有个体化建议泄漏，不能依靠事后撤回 |
| 用户按了接受，下一轮就记已用药 | 实际用药需单独来源；重复、延迟和更正均影响当前证据有效性 |
| 双 reviewer 通过，所以等同临床复核 | 同底模分角色审核不等于独立证据或医师审核，规则与用途验证仍必需 |
| 所有输入均被挡住，所以 Harness 很安全 | 正常有效任务也须完成，记录误拒率、解释质量、耗时及费用 |
| 研究数据成功，所以真人服务可上线 | 仿真、用户任务与实际临床用途的证据分别验收，不靠标签切换升级 |

验收用例和必要实现已写回 01 第 7.1–7.3 节；该列表不是新的已完成清单。

## 7. 下一步与剩余不确定性

下一步只做 **P01：落实首版输入、动作、证据与用途配置合同**。先把公开仿真场景和真实模型未来必须提供的字段定准确，再暴露预测/RL MCP 工具和主 Agent 循环。避免在模糊的“给药建议”之上固定错误接口。

具体预测器训练域/窗口、公开资源取得与逐字段质量、真实 RL 效果、医学规则来源与有效性，均尚无本次运行验证。真实药品/装置和患者用途不能由通用仿真配置自动推出。面向真实患者给药的最终产品，针对真实用途的技术与临床评价以及适用准入需要单独完成；本轮不申请医院私有数据，也不将其列为研究实现依赖。临床评价的一般框架可参考 [IMDRF](https://www.imdrf.org/documents/software-medical-device-samd-clinical-evaluation)，并非已确定本项目的具体监管结论。

v6 补齐了已发现的主要产品链路和可追踪验收项，足以开展下一项合同实现；它仍是待逐项证实的计划，不能宣布“已完备且保证产品成功”。旧已完成 TODO 保留；新增软件要求均为未完成。
