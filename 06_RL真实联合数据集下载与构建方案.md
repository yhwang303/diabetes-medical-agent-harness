# RL 真实联合数据集：最终下载与构建方案

> 2026-09-14更新：Loop已完成无插值预处理，最终条件性离线训练清单见 [批次44最终审计](Loop数据集/预处理_v1/最终审计报告.md)。train225人/1,653,421条transition；四组总329人。本报告下方的原始审计数字/369提名上限保留为历史，不能替代处理后的训练人数。

> 2026-09-14目录更新：Loop专属原始数据和历史材料已迁至 [Loop数据集](Loop数据集/00_从这里开始.md)。本轮仅整理目录与逐文件/逐字段说明，未执行事件重建、资格划分或训练；下文历史目录示意不代表当前Loop存放位置。

2026-09-12 批次38更新｜已完整解压五来源并审计关键原始表。当前最终决定与实际数字以 [07 真实数据集RL训练资格审计报告](07_真实数据集RL训练资格审计报告.md) 为准：Loop为唯一首版RL/生理主池候选，其他源用途分层。未正式插值/对齐、未训练、未改Harness。下方保留取数和方法依据，旧候选分工已按实物审计修订。

## 1. 最终决定

**用Loop合格成人速效泵轨迹作为首版RL和生理患者模型主来源；不将五源无损拼成一个同质RL训练集。** Loop成人CGM+basal+bolus+food+体重字段交集454人，进一步问卷筛选提名上限369人；均不是最终合格训练人数，仍需事件重建与片段资格冻结。

- **主开发：Loop合格动作池，以及更严格的餐食/体重/可辨识生理池。** BC→TD3+BC离线基线；ReplayBG公开multi-meal生理结构作患者模型基线，通过独立验证后才开展受约束模型式RL。
- **预测/模态补充：DCLP3、Ohio、UOM的对应合格片段。** DCLP3只有命令基础率且缺CHO，Ohio真实体重缺失，UOM基础事件冲突与持续语义未解决，均不默认进入首版M/RL主池。已有F训练重叠须核对。
- **IOBP2整来源外部保留。** 标准BP成年107人是同制剂候选，Fiasp另列；仅作数据支持的事实预测迁移、动作支持/缺失/OOD压力检查。无CHO，不能宣称完整反事实闭环验证；不参与预训练、调参或归一化。
- simglucose/GluCoEnv仅辅助生理场景；后者仍为2008底座，不作为唯一验收环境。MetaboNet/DIAX仅字段/处理线索，不重复追加同源患者。
- DiaTrend继续暂停，不再扩下载名单。若Loop事件重建后不满足主池资格，暂停模型式主线，不用其余来源填零凑齐。

模型尚未训练，Harness须新增真实来源/制剂/basal+bolus与证据等级合同后接入研究模式；不开放真人给药。本轮完整证据见07及datasets/audit/2026-09-12/。

## 2. RL-DITR 原文到底做了什么

已读取用户提供 PDF 共 30 页中的相关内容，渲染核对 PDF 第 11–12 页 Methods，以及第 16 页 Extended Data Fig. 1。来源：[本地原论文](参考论文/RL-DITR_NatureMedicine_2023.pdf)、[正式论文](https://www.nature.com/articles/s41591-023-02552-9)、[作者仓库](https://github.com/rlditr23/RL-DITR)。

**论文没有采用 simglucose/UVA-Padova 那类预先编写代谢方程的机理生理仿真器。它确实训练了一个 patient model，具体由数据驱动的动态转移和状态预测组成。** 之前把两者都笼统称作“生理模型”，容易造成误解；后续统一使用下列术语。

| 名称 | 职责 | 本项目对应 |
| --- | --- | --- |
| 机理生理模型 | 用代谢方程及参数计算餐食/胰岛素后果 | 本项目选择ReplayBG公开结构作为M基线；参考论文没有使用此类结构 |
| 动作条件患者模型 M | 从历史辨识“状态＋给药动作→后续状态/血糖/奖励” | 先辨识生理结构参数、独立验证，再决定是否加受限残差；是模型式RL环境 |
| 历史预测器 F | 从已观测历史预测未来趋势；当前已有模型不接未来候选动作 | 提供趋势特征；不能直接替代动作条件患者模型 |
| 策略 π | 从状态选择给药动作 | 在 M 中学习，结合真实历史行为约束和独立评价 |

按原文记号：

1. **历史表示**：`s_t = f_R(O_1:t)`，把历史观测编码成隐状态。
2. **动态转移**：`(s_{t+1}, r_t) = f_T(s_t, a_t)`，明确接收给药动作。
3. **状态预测**：`y_t = f_P(s_t)`，预测血糖及是否处于目标范围。
4. **策略**：`a_t ~ π(.|s_t)`，生成动作，再交回患者模型继续展开。

这里的 `patient model = f_T + f_P`，共享表示编码器 `f_R`。预测头本身读取隐状态，隐状态会被先前给药动作更新；不能把 `f_P(s)` 简化成一个与动作完全无关的固定未来血糖曲线。

原文训练分两阶段：先联合训练表示、动态转移及预测（包括转移一致性、血糖预测和 WTR 分类目标）；再训练策略，结合历史轨迹、患者模型生成轨迹的策略梯度与医生动作监督。表示和动态转移使用 Transformer，预测/策略/价值使用 MLP。**本文没有把 PPO 指定为其原始算法；PPO+行为约束是我们可选择的工程实现，不能写成论文原方法。**

原研究为住院 T2D、每日七个时段，开发队列排除了泵使用者。我们的 CGM/成人 T1D/基础输注任务借鉴其方法，不复刻原人群、给药动作或临床结论。真实数据驱动的 M 同样可能学到混杂和外推错误，不能仅靠 M 内奖励证明有效。

## 3. 当前五个下载来源与暂停项

### 3.1 原下载入口与完整包要求（实际资格以07为准）

| 来源 | 官方入口及版本 | 下载什么 | 后续筛选与用途 |
| --- | --- | --- | --- |
| **Loop Observational Study** | [Jaeb 下载表单](https://public.jaeb.org/dataset/560)；官方目录列 `Loop study public dataset 2023-01-31.zip` | 完整原始 ZIP、README/字典、受试者表、设备/治疗说明、条款；包内 CGM、basal、bolus、food 等文件均保留 | 成人且有可信配对泵事件的轨迹，作为首版主来源候选；当前成人交集以07为准。公开整理器列 845 个ID，原主要研究为558名启动者，不能把任何数字当合格成人数 |
| **DCLP3** | [Jaeb 下载表单](https://public.jaeb.org/dataset/573)；`DCLP3 Public Dataset - Release 3 - 2022-08-04.zip` | 完整 Release 3 ZIP、数据字典/README、人口及分组表、全部 CGM/泵事件；研究 protocol 可一并保存 | 筛年龄≥18、治疗/模式明确、CGM与基础/bolus完整配对的轨迹。原 RCT 168人、某些整理版112人不是最终合格人数 |
| **T1D-UOM V1.0.4** | [Zenodo记录](https://zenodo.org/records/17361905)、[固定仓库版本](https://github.com/sharpic/ManchesterCSCoordinatedDiabetesStudy/tree/V1.0.4) | 固定版本完整包；CGM、basal、bolus、营养、活动、睡眠、demographics、schema、README、根目录LICENSE全部保留 | 7名泵用户中6人有basal文件，逐人逐段审核；多模态补充。不能混入长效注射，不能默认基础率是实际递送 |

Jaeb 的公开表单需要姓名、邮箱、机构（没有可填 `none`）、≤500字符研究用途以及接受条款；已按用户授权登记并取得Loop/DCLP3原包。以表单实际交付的版本为准，若文件版本不同，保留原名和获取日期，分析时核对。

可供用户按实际研究目的调整的 Jaeb 英文用途（少于500字符）：

> Non-commercial research on adult type 1 diabetes. We will assess paired CGM and insulin-pump records, develop action-conditioned patient models and offline reinforcement-learning methods, and evaluate generalization across independent participants and datasets. This is retrospective research; no treatment will be delivered to patients.

### 3.2 Ohio与IOBP2已取得；以下保留获取路径与初筛依据

2026-09-12追加核验：用户提供[作者新下载页](https://webpages.charlotte.edu/rbunescu/data/ohiot1dm/OhioT1DM-download.html)，已取得加密官方ZIP。其24个XML与Kaggle合并包目录/大小/声明CRC32完全对应；密码尚未验证成功，因此明文SHA256一致性仍待补核。详见[datasets/access/Ohio官方与Kaggle对比.md](datasets/access/Ohio官方与Kaggle对比.md)。不增加患者、不重复入池。

| 来源 | 获取方式与资料 | 用途与限制 |
| --- | --- | --- |
| **OhioT1DM 2018+2020** | 已直接下载[Kaggle镜像](https://www.kaggle.com/datasets/ryanmouton/ohio-data-set)，两年各6人、各12个原文件格式XML，合计24文件；完整保留平台ZIP。作者请求邮件已在前轮发送，不重复发送或以等待回信阻塞镜像下载 | 多模态补充。镜像与平台全量清单及传输校验一致，2018部分与此前镜像完全相同、不能重复入池；尚未与官方交付逐字节比对，原发布方许可及配套说明待核对。此前ICML用过Ohio，尤其要核现有F的训练患者重叠 |
| **IOBP2** | [Jaeb官方表单579](https://public.jaeb.org/dataset/579)，`IOBP2 RCT Public Dataset.zip`；普通姓名/邮箱/机构/用途登记及数据条款，无Synapse身份文件流程。保留全部原包、字典、CRF、协议和说明 | **替代DiaTrend，拟整来源外部留出**。官方字典列出CGM、上一执行步已递送基础分量/bolus分量/餐时分量，以及年龄、研究组和制剂等资料。已完成原始审计：标准BP成人107人候选，缺CHO；只作外部部分评价，净片段资格未冻结 |
| **DiaTrend（暂停）** | [Synapse记录](https://doi.org/10.7303/syn38187184)；用户因身份验证手续过重决定不继续，未取得数据。既有申请材料保留历史 | 不作为当前取数或训练依赖，不继续索取身份证明；原方案以其作为外部源的安排由IOBP2候选取代 |

**IOBP2必须另核的动作边界：** `BasalDelivPrev`、`BolusDelivPrev`和`MealBolusDelivPrev`是上一步的剂量/分量，不可直接当作U/h；核对实际执行间隔、上一执行步对齐、分量是否重复包含，本轮已核实三分量相加为总量（760行异常须隔离），仍不能跳过时序/连续性直接求和作标签。保留设备模式、餐食相对大小提示、手工注射、断泵/转换阶段和制剂。不能把餐食提示当精确碳水克数，也不宣称含活动/睡眠等完整可穿戴模态。不同制剂不静默混用。全部训练之前决定资格与来源留出，后续不按模型分数挑测试人群。

**替代来源本轮结论：**当前AIDE T1D公开包没有基础/bolus事件时序，HYCLO只有设备/访视汇总，当前FLAIR的胰岛素表是每日汇总，三者不补入。HUPA-UCM预处理版有基础量摊分与缺失补零，原始泵事件完整性未核验，暂不列为替代；不能据此断言其原始数据永远不适用。详细官方目录与字典初筛见[datasets/access/2026-09-12/alternative-metadata](datasets/access/2026-09-12/alternative-metadata)。宁缺毋滥，不为凑来源降低资格。

**Ohio官方路径与镜像分开记录：**[作者详细获取页](https://webpages.charlotte.edu/rbunescu/data/ohiot1dm/OhioT1DM-dataset.html)及[旧入口](https://webpages.charlotte.edu/rbunescu/ohiot1dm.html)的获取/DUA说明仍须遵循；不把第三方平台许可证当原发布者授权证明。

第一轮不要求下载 AZT1D、BrisT1D-Open、DCLP5、CGMacros、Insulin4RL 或原 RL-DITR 私有医院数据。前两者的动作加工限制、儿童/其他人群及非给药字段等原因见 [来源审核](05_公开数据集与RL闭环可行性审核.md)。这些原有候选保留在历史调研中，未被宣称完成或删除要求。

## 4. 文件放置：完整保留原包

本项目已准备目录 `datasets/raw/`，每个来源单独保存：

```text
datasets/raw/
  loop/
  dclp3/
  t1d_uom/
  ohiot1dm/
  iobp2/
  diatrend/  # 暂停，无患者数据
```

请把网站提供的原始压缩包和配套说明放入对应目录。原始压缩包即使解压也保留；不要先用 Excel 重新保存、重采样、删列、补零或跨文件拼接。许可证、批准的用途/使用条件应保留；个人申请信息单独私下保存，不写入公开报告。

允许下载多少取决于条款；取得使用权不自动允许把联合原始数据再分发。未来可交付处理代码、字段字典、统计和许可允许的模型/产物，受控原包保持受控。

## 5. 下载后如何形成真正可训练的数据集

### 5.1 固定目标与三种资格

任务固定为成人 T1D 皮下泵基础输注研究，默认5分钟决策。RL只选择基础率；患者模型必须保留并解释历史 bolus、暂停及其他实际胰岛素暴露，不采用“真实数据中的bolus全部置零”的旧仿真设定。未来 bolus/餐食不得作为决策时已知特征泄露。

患者/连续片段资格分层（当前字段存在交集不是最终资格）：

| 资格 | 必须满足 | 用途 |
| --- | --- | --- |
| A：动作池 | 成人速效泵；CGM与basal/bolus及连续前史可信，动作时段/单位/证据等级明确 | BC、受限离线RL；首版只用Loop合格段 |
| M：生理池 | A基础上再有真实体重/制剂、较可信CHO量/时间、充分连续和参数可辨识性 | ReplayBG辨识及后续模型式RL；当前只有Loop候选 |
| P：预测/模态池 | 对相应CGM/模态任务质量合格；不要求伪造缺少的动作或餐食 | 预测/可选模态，不能给动作损失填伪标签 |
| E：外部保留 | 整来源不训练，按实际支持的指标评价 | IOBP2标准BP与Fiasp分层；不能代表完整反事实验证 |
| X：排除 | 人群/暴露/时序不适用或无法解释 | 保留排除理由，不进入学习 |

Loop原字典/Tidepool确认实际基础区间，仍须解决重复重叠和停泵语义；DCLP3为CommandedBasalRate且缺区间确认。命令/推算暴露不得改为现有Harness的actual_delivery。没有CHO克数/真实体重不能凭A级动作资格自动获M资格。

### 5.2 统一 schema，但保留原始事件层

至少保留三层：不可改的原包 → 标准事件表 → 从事件表派生的模型轨迹。处理代码和规则版本绑定每次派生结果。

| 字段组 | 内容 |
| --- | --- |
| 身份/来源 | dataset_id、source_subject_id、全局患者分组、episode_id、文件定位、版本/hash、许可标识 |
| 时间 | 观测/事件时间、记录可用时间、原时区或未知标记、持续时间、CGM采样与缺失mask |
| 核心生理/动作 | CGM及原/标准单位；基础率事件；区间基础量；独立bolus及其持续时间/类型；暂停/恢复；动作证据等级 |
| 可选上下文 | 碳水、心率、活动、睡眠、治疗/泵模式、可公开的人口资料，各自带来源/缺失/可用时间 |
| 派生标识 | 重采样/转换/插补版本，是否推算，质量等级，排除原因，split |

初始研究协议：5分钟网格，6小时历史作为初始输入候选；优先构建连续至少24小时的主轨迹，保留完整原始时间线用于延迟作用/长期评价。6小时是待训练前验证的研究配置，不是医学充分性保证；真实F若不支持该窗口，应通过显式适配或另行冻结输入，不能硬截断假装兼容。

剂量按照实际事件时间积分分配；不能将每个时间格的最后一条基础率直接视为全格执行。bolus不前向填充为基础率；CGM不跨长缺口插值。无法区分“没有事件”与“记录缺失”的片段不进入核心动作研究集。历史可选模态不全不等于排除，但也不能把缺餐食标成0克；将缺失模式纳入分析，检查它是否与设备或行为策略相关。

每个模型 transition 只连接同一患者连续有效片段中的相邻决策；保存 `h_t / action_t / next_observation / reward / terminated / truncated / action_source`，时间截断与生理失败分开。不会把不同患者或有缺口的片段首尾接成一个episode。

## 6. 固定独立划分方案

**外部来源候选：IOBP2 合格成人iLet基础输注轨迹全部留出。** 下载后的字典/缺失/质量核验可以做，但不查看模型成绩来决定是否留下，不用它挑模型、阈值、奖励或超参数。它只有通过字段资格才构成有效外部评价源；设备表存在不等于任何确定人数已经完整可评；仍须核对跨来源重叠、人口/制剂/动作语义、连续性及质量。

**开发资源池：Loop主动作/生理候选，加DCLP3/Ohio/UOM相应预测/模态合格患者；用途分层，不自动共同训练RL。** 按唯一患者分组，固定种子20260911，目标比例70%训练、15%验证、10%内部封存测试、5%演示。按来源、可用的年龄分层和设备/治疗模式尽可能平衡；小来源不强求每个分组都有人，保存实际人数及偏离目标比例的原因。比例按患者而非行数计算。

先核验原数据是否有必须遵循的既定划分/使用条件。若允许本项目患者级重分配，保留原文件split标签并另建本项目split；若不允许，则优先遵循该条件，将本源单独作为外部基准或排除，不强行凑比例。尤其 Ohio 原预测竞赛文件不能简单当本项目的患者独立划分。

所有F/编码器/M/行为参考/策略及数据驱动预处理共用隔离边界。已被现有F用于训练的患者只能进入训练池，不能用于全链独立测试；F的预训练来源无法核清时，应重新训练可追溯版本或明确不能声明全链独立性。

检查聚合源与原源重复、同研究不同release及可能的重复参研；不得通过重新编号声称已去重。去标识化信息不足以证明跨研究无重叠时，将已知关联来源按组隔离，保留残余不确定性，不做身份重识别。训练采样先抽来源/患者，再抽片段，并用训练池/验证集确定合理权重，避免Loop或长记录患者完全主导。

**原外部备用规则已被本次审计替代：**IOBP2与DCLP3均缺CHO，DCLP3另为命令基础率，不能自动互相替代完整反事实评价。IOBP2保持外部部分评价；完整真实反事实证据仍缺，依靠内部患者留出事实评价与独立生理场景分别检验相应问题。若外部字段不支持某项指标，该项保持未完成，不能换名称制造通过。

正式Agent self-evolution仍按项目已有evolve_train/evolve_validation/sealed_test/demo协议另行开展，不能因本次模型训练划分就提前启动。

## 7. 模型训练与 Harness 接入的边界

首版顺序已按实物审计确定：**Loop资格冻结 → BC与TD3+BC离线基线；Loop严格M池的ReplayBG结构辨识/独立验证 → 受行为/支持度/不确定性约束的模型式RL → 独立多日场景和分层真实留出评价。** 既有F经适配/追溯或重训后提供趋势，不能用与候选动作无关的固定F曲线给任意剂量评分。M以生理结构起步，不预设黑箱残差必然必要；不承诺创新算法收益。配套餐时/纠正/救援规则须随新模拟历史反馈，不能在改变basal后原样回放未来真实CGM/自动bolus。

真实泵策略往往为AID或人工调节，行为不是随机分配；必须保留设备模式、可用决策上下文与动作覆盖。数据量增加不能自动消除混杂。M 的外部预测误差、数据支持度、可诊断的离线策略评价及独立闭环试验分别报告；不让训练M成为唯一裁判，不把回放历史CGM当执行了新策略。

现有Core/MCP/独立worker可承接最终模型，但当前真实模型配置为空，基础输入只支持合成/仿真资格。真实回顾性用途、历史bolus及命令/推算/实际递送等级需要独立版本合同，不能修改标签绕过闸门。个体化给药仍需有效F与RL父子证据、适用性、安全、审核及唯一发布；真实数据训练不自动授予真人使用资格。

## 8. 本轮审计交付与下一项

2026-09-12已完成：[07总报告](07_真实数据集RL训练资格审计报告.md)、Loop/Trials/Ohio-UOM专项报告、文献复核、Harness合同差距。五来源512文件解压/完整性验证，全部关键事件表原始质量审计。脚本、原始分布统计、逐文件清单位于datasets/audit/2026-09-12/，原ZIP不变。

**下一项只做Loop事件重建与患者/片段资格冻结。** 需要来源/文件hash、事件恢复证据、完整前史/餐食可信度、患者独立与F重叠核验、正式split_manifest，及净患者/人日/episode/transition。当前CGM连续性和字段存在交集不是净训练规模。本轮按用户要求不正式插值/对齐、不生成训练表、不建模。

## 9. 当前完成状态

| 项目 | 状态 |
| --- | --- |
| 原论文与生理/RL方法复核 | ☑️ 总报告与文献分报告已完成 |
| 五来源完整包取得、解压与核心表审计 | ☑️ Loop/DCLP3/UOM/IOBP2及Ohio两年镜像；官方Ohio仍未解密，不能声称明文逐字节比对完成 |
| 来源用途和首版训练顺序 | ☑️ 以07及本版分工为准 |
| 正式事件重建、患者/片段资格与独立划分 | ☐ 未执行；提名人数不是合格人数 |
| 真实M/策略训练、独立评价与Harness接入 | ☐ 未执行，M01/M02父项未完成 |

额外一手线索：[Jaeb官方目录](https://public.jaeb.org/datasets/diabetes)、[UVA机构DIAX转换器](https://github.com/Center-for-Diabetes-Technology/DIAX)、[MetaboNet统一数据格式方法](https://arxiv.org/html/2601.11505v1)。机构转换代码只作为字段定位线索，不能替代原数据字典或授予复制/再分发许可。
